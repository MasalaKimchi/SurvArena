---
phase: 01-deterministic-execution-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - survarena/benchmark/runner.py
  - survarena/logging/export.py
  - tests/test_benchmark_runner.py
autonomous: true
requirements:
  - EXEC-04
must_haves:
  truths:
    - "User can resume an interrupted run and keep valid completed work without rerunning it."
    - "User retries failed rows only within configured retry budget."
    - "User can inspect structured failure records for failures that still fail after retries."
  artifacts:
    - path: "survarena/benchmark/runner.py"
      provides: "Resume eligibility validator and retry-budget-aware skip/retry decisioning"
    - path: "survarena/logging/export.py"
      provides: "Persisted structured run ledger/failure artifacts with required status fields"
    - path: "tests/test_benchmark_runner.py"
      provides: "Automated resume/failure semantics coverage"
  key_links:
    - from: "survarena/benchmark/runner.py"
      to: "existing fold results csv"
      via: "resume eligibility filter before completed-key insertion"
      pattern: "status.*success"
    - from: "survarena/benchmark/runner.py"
      to: "survarena/logging/export.py"
      via: "run_records payload export"
      pattern: "export_run_ledger\\("
---

<objective>
Implement strict resume eligibility and structured failure-preservation behavior so interrupted benchmark runs recover safely without discarding successful work.

Purpose: Deliver EXEC-04 with locked decisions D-06 and D-07 by making resume completion checks integrity-aware and retry/failure handling auditable.
Output: Resume eligibility validator, bounded retry semantics, preserved failure ledgers, and dedicated resume tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-deterministic-execution-foundation/01-CONTEXT.md
@.planning/phases/01-deterministic-execution-foundation/01-RESEARCH.md
@.planning/phases/01-deterministic-execution-foundation/01-PATTERNS.md
@survarena/benchmark/runner.py
@survarena/logging/export.py
@tests/test_compare_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resume eligibility validator for completed-key preservation</name>
  <read_first>
    - survarena/benchmark/runner.py
    - survarena/logging/export.py
    - tests/test_compare_api.py
    - .planning/phases/01-deterministic-execution-foundation/01-CONTEXT.md
    - .planning/phases/01-deterministic-execution-foundation/01-RESEARCH.md
  </read_first>
  <files>survarena/benchmark/runner.py, tests/test_benchmark_runner.py</files>
  <behavior>
    - Test 1: Resume includes row in completed set only when `status=success` and required outputs are valid.
    - Test 2: `status=success` row missing required fields/metrics is treated as incomplete and re-executed.
    - Test 3: Non-success rows are never treated as completed keys.
  </behavior>
  <acceptance_criteria>
    - Resume completed-key insertion uses explicit validation, not status-only checks (D-06).
    - Required fields for completion are centralized in one validator/helper and reused by resume logic.
    - Invalid or incomplete prior outputs are logged as ineligible and not skipped.
  </acceptance_criteria>
  <action>Create failing resume-eligibility tests in `tests/test_benchmark_runner.py` first, then implement a dedicated validator in `survarena/benchmark/runner.py` that requires `status=success` plus non-empty required identifiers/metric fields before adding keys to `completed_keys`; ensure incomplete rows fall back to normal execution path rather than being skipped, and keep decision traceability comments/messages referencing D-06 semantics. Follow `01-PATTERNS.md` analogs in `survarena/benchmark/runner.py` for "Resume-eligibility seed key pattern" and apply the shared "Resume filtering via key set + status gate" pattern, extending it with required-output integrity checks.</action>
  <verify>
    <automated>pytest tests/test_benchmark_runner.py -k "exec04" -x</automated>
  </verify>
  <done>Resume logic skips only integrity-valid successful rows and re-runs corrupt/incomplete rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve retry-budget and structured failure records through export</name>
  <read_first>
    - survarena/benchmark/runner.py
    - survarena/logging/export.py
    - tests/test_benchmark_runner.py
    - .planning/phases/01-deterministic-execution-foundation/01-CONTEXT.md
  </read_first>
  <files>survarena/benchmark/runner.py, survarena/logging/export.py, tests/test_benchmark_runner.py</files>
  <behavior>
    - Test 1: Failed rows retry until `max_retries` and stop afterward with final failed status preserved.
    - Test 2: Each attempt keeps `retry_attempt` and failure metadata in exported run records.
    - Test 3: Successful retries still preserve prior failed-attempt evidence in structured ledger.
  </behavior>
  <acceptance_criteria>
    - Retry behavior remains bounded by configured budget and does not drop failed-attempt evidence (D-07).
    - Run ledger export contains structured per-attempt `status`, `retry_attempt`, and failure payload fields.
    - Resume flow does not collapse or deduplicate attempt history in a way that loses failure diagnostics.
  </acceptance_criteria>
  <action>Add failing retry/failure-preservation tests in `tests/test_benchmark_runner.py`, then adjust runner/export interactions so every attempt record (including failures before eventual success) is retained with explicit `retry_attempt`, `status`, and failure details in run ledger outputs; enforce stop condition at retry budget edge per D-07 and preserve machine-readable diagnostics for post-run inspection. Follow `01-PATTERNS.md` runner/export analogs ("Retry loop + structured attempt tracking", "Structured failure records instead of hard crash", and "File export through logging tracker helpers").</action>
  <verify>
    <automated>pytest tests/test_benchmark_runner.py -k "exec04" -x</automated>
  </verify>
  <done>Failure attempts and retry boundaries are preserved in ledger artifacts and validated by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Harden phase-level resume regression coverage</name>
  <read_first>
    - tests/test_benchmark_runner.py
    - survarena/benchmark/runner.py
    - survarena/logging/export.py
    - .planning/phases/01-deterministic-execution-foundation/01-VALIDATION.md
  </read_first>
  <files>tests/test_benchmark_runner.py</files>
  <acceptance_criteria>
    - Test module covers interrupted-run resume, eligibility filtering, retry cap, and failure artifact preservation.
    - Test names and fixtures map directly to EXEC-04 success criteria language.
    - Suite runs deterministically in CI-friendly local temp directories.
  </acceptance_criteria>
  <action>Consolidate/extend `tests/test_benchmark_runner.py` with deterministic temp-dir fixtures and explicit assertions for EXEC-04 behaviors (preserve successful outputs, retry failed rows within budget, retain structured failure records), and remove redundant assertions that do not validate resume contract outcomes. Follow `01-PATTERNS.md` test analog `tests/test_compare_api.py` and shared "Test style: monkeypatch + tmp_path + focused asserts" pattern.</action>
  <verify>
    <automated>pytest tests/test_benchmark_runner.py -x</automated>
  </verify>
  <done>Resume regression suite fully exercises EXEC-04 contract and passes consistently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prior artifact files -> resume decisioning | Persisted fold rows can be incomplete/tampered and must be validated before skipping work |
| Runner payloads -> exported ledgers | Attempt/failure evidence must remain intact across retry/export boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `survarena/benchmark/runner.py` resume filter | mitigate | Validate required output integrity before considering rows complete; treat invalid success rows as rerun candidates (D-06). |
| T-01-05 | R | retry + export path (`runner.py`, `export.py`) | mitigate | Persist per-attempt status/retry/failure details without deduping away failed attempts (D-07). |
| T-01-06 | D | retry loop | accept | Retry budget already bounds compute amplification; local CLI scope and explicit `max_retries` control keep risk acceptable. |
</threat_model>

<verification>
- `pytest tests/test_benchmark_runner.py -x` (full module: 17 tests)
- `pytest tests/test_benchmark_runner.py -k "exec04" -x` (EXEC-04 focus: 6 tests)
</verification>

<success_criteria>
- Resume skips only integrity-valid successful rows and re-runs incomplete/corrupt rows.
- Retry behavior respects configured budget and preserves attempt history.
- Structured failure records remain available for inspection after resumed runs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-deterministic-execution-foundation/01-02-SUMMARY.md`
</output>
