---
phase: 01-deterministic-execution-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - survarena/run_benchmark.py
  - survarena/benchmark/runner.py
  - survarena/data/splitters.py
  - configs/benchmark/standard_v1.yaml
  - tests/test_benchmark_runner.py
autonomous: true
requirements:
  - EXEC-01
must_haves:
  truths:
    - "User can run smoke, standard, and manuscript benchmark tiers with deterministic defaults."
    - "User receives a hard failure when an existing split manifest does not match expected deterministic payload."
    - "User can explicitly regenerate splits instead of silent automatic regeneration."
  artifacts:
    - path: "survarena/run_benchmark.py"
      provides: "CLI-level strict profile and split-governance control flags"
    - path: "survarena/data/splitters.py"
      provides: "Manifest mismatch hard-fail path and explicit regeneration handling"
    - path: "tests/test_benchmark_runner.py"
      provides: "Automated coverage for profile contract and deterministic split governance"
  key_links:
    - from: "survarena/run_benchmark.py"
      to: "survarena/benchmark/runner.py"
      via: "validated benchmark config and explicit regeneration option"
      pattern: "run_benchmark\\("
    - from: "survarena/benchmark/runner.py"
      to: "survarena/data/splitters.py"
      via: "load_or_create_splits call with strict deterministic options"
      pattern: "load_or_create_splits\\("
---

<objective>
Enforce Phase 1 deterministic execution guarantees so profile-tier runs are locked to canonical behavior and split governance is explicit and auditable.

Purpose: Satisfy EXEC-01 and locked decisions D-02, D-03, D-04, and D-05 before downstream HPO/statistical phases depend on these execution contracts.
Output: Strict profile validation, hard-fail split-manifest mismatch semantics, explicit regeneration control, and deterministic governance tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-deterministic-execution-foundation/01-CONTEXT.md
@.planning/phases/01-deterministic-execution-foundation/01-RESEARCH.md
@.planning/phases/01-deterministic-execution-foundation/01-PATTERNS.md
@docs/protocol.md
@survarena/run_benchmark.py
@survarena/benchmark/runner.py
@survarena/data/splitters.py
@configs/benchmark/smoke_all_models_no_hpo.yaml
@configs/benchmark/standard_v1.yaml
@configs/benchmark/manuscript_v1.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict profile-tier contract validation at benchmark entry</name>
  <read_first>
    - survarena/run_benchmark.py
    - survarena/benchmark/runner.py
    - configs/benchmark/smoke_all_models_no_hpo.yaml
    - configs/benchmark/standard_v1.yaml
    - configs/benchmark/manuscript_v1.yaml
    - .planning/phases/01-deterministic-execution-foundation/01-CONTEXT.md
    - .planning/phases/01-deterministic-execution-foundation/01-RESEARCH.md
  </read_first>
  <files>survarena/run_benchmark.py, survarena/benchmark/runner.py, tests/test_benchmark_runner.py</files>
  <behavior>
    - Test 1: Benchmark config with profile `smoke` + expected deterministic fields passes validation.
    - Test 2: Benchmark config with profile `standard` + canonical deterministic fields passes validation.
    - Test 3: Any profile not in {smoke, standard, manuscript} raises ValueError with actionable message.
    - Test 4: Missing deterministic seed/split fields raises ValueError describing missing keys.
  </behavior>
  <acceptance_criteria>
    - A dedicated validator enforces canonical profile intent per D-02 and D-03 before run execution starts.
    - The validator checks deterministic split/seed keys and rejects missing or malformed values per D-05.
    - Validation errors include the invalid profile/field name and the accepted contract values.
  </acceptance_criteria>
  <action>Create deterministic profile contract tests first in `tests/test_benchmark_runner.py`, then implement a reusable strict validation helper invoked from the benchmark entry path (`run_benchmark.py` and/or runner setup) so benchmark runs fail fast when profile intent or deterministic defaults are violated; implement exact profile set `{smoke, standard, manuscript}` per D-03 and explicit required deterministic keys (`split_strategy`, `seeds`, profile-specific fold/repeat fields) per D-02 and D-05. Follow `01-PATTERNS.md` by reusing the run-entry CLI contract analog in `survarena/run_benchmark.py` plus the shared "Fail-fast validation with explicit errors" pattern used in `survarena/data/splitters.py`.</action>
  <verify>
    <automated>pytest tests/test_benchmark_runner.py -k "profile_contract" -x</automated>
  </verify>
  <done>Invalid profile-tier configurations are rejected before any model execution, and deterministic profile-contract tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce split-manifest mismatch hard failure with explicit regeneration</name>
  <read_first>
    - survarena/data/splitters.py
    - survarena/run_benchmark.py
    - survarena/benchmark/runner.py
    - tests/test_benchmark_runner.py
    - .planning/phases/01-deterministic-execution-foundation/01-CONTEXT.md
  </read_first>
  <files>survarena/data/splitters.py, survarena/run_benchmark.py, survarena/benchmark/runner.py, tests/test_benchmark_runner.py</files>
  <behavior>
    - Test 1: Existing manifest payload mismatch raises deterministic contract error by default.
    - Test 2: Explicit regenerate option bypasses mismatch failure and writes fresh split artifacts.
    - Test 3: Matching manifest payload still reuses existing splits and keeps deterministic integrity checks.
  </behavior>
  <acceptance_criteria>
    - Manifest mismatch no longer falls through to silent regeneration in default mode (D-04).
    - Regeneration requires explicit operator intent through a clear runtime switch (D-04).
    - Runner passes strict/governed split policy through every split load path for smoke/standard/manuscript tiers.
  </acceptance_criteria>
  <action>Add failing mismatch/regeneration tests to `tests/test_benchmark_runner.py`, then modify `load_or_create_splits` flow in `survarena/data/splitters.py` to raise an explicit deterministic-manifest error when payloads differ unless a new explicit regeneration flag is enabled; wire this control from CLI to runner to splitter call sites so behavior is scriptable and visible, and preserve existing split integrity/stratification checks for the reuse path. Follow `01-PATTERNS.md` analogs in `survarena/data/splitters.py` (deterministic manifest payload + manifest reuse gate) and keep the shared fail-fast ValueError style.</action>
  <verify>
    <automated>pytest tests/test_benchmark_runner.py -k "manifest_mismatch or regenerate" -x</automated>
  </verify>
  <done>Manifest mismatch is a hard failure unless explicit regeneration is requested, and deterministic split-governance tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Lock standard profile naming/config to canonical intent</name>
  <read_first>
    - configs/benchmark/standard_v1.yaml
    - configs/benchmark/smoke_all_models_no_hpo.yaml
    - configs/benchmark/manuscript_v1.yaml
    - .planning/phases/01-deterministic-execution-foundation/01-CONTEXT.md
    - docs/protocol.md
  </read_first>
  <files>configs/benchmark/standard_v1.yaml, tests/test_benchmark_runner.py</files>
  <acceptance_criteria>
    - `standard_v1` profile metadata is represented as canonical `standard` intent (not alternate labels) per D-03.
    - Deterministic profile-contract tests assert the canonical tier intent for smoke/standard/manuscript configs.
    - Existing benchmark invocation paths continue to load configs without changing benchmark IDs.
  </acceptance_criteria>
  <action>Update `configs/benchmark/standard_v1.yaml` profile field/value to canonical standard tier naming per D-03, keep benchmark identity and runtime controls unchanged, and extend deterministic tests to assert profile-tier intent normalization across all three benchmark config files without introducing compatibility-breaking benchmark-id changes. Follow `01-PATTERNS.md` config analog mapping (`standard_v1.yaml` <-> `manuscript_v1.yaml`) and the shared test style pattern (focused `pytest` assertions with explicit failure messages).</action>
  <verify>
    <automated>pytest tests/test_benchmark_runner.py -x</automated>
  </verify>
  <done>All benchmark tier configs align to canonical profile intent and deterministic contract tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI config input -> benchmark runtime | Untrusted config values determine deterministic policy and split behavior |
| Split cache files -> runtime split loader | On-disk manifests can drift or be tampered, impacting comparability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `survarena/data/splitters.py` manifest reuse gate | mitigate | Raise deterministic contract error on payload mismatch unless explicit regeneration flag is set (D-04). |
| T-01-02 | R | `survarena/run_benchmark.py` profile validation | mitigate | Fail fast with explicit profile/field error messages and accepted canonical values to preserve auditable run intent (D-02, D-03, D-05). |
| T-01-03 | D | split regeneration control path | accept | Regeneration remains operator-triggered and local-process bounded; no remote attack surface in this phase scope. |
</threat_model>

<verification>
- `pytest tests/test_benchmark_runner.py -x`
- `python -m survarena.run_benchmark --benchmark-config configs/benchmark/smoke_all_models_no_hpo.yaml --dry-run`
- `python -m survarena.run_benchmark --benchmark-config configs/benchmark/standard_v1.yaml --dry-run`
</verification>

<success_criteria>
- Benchmark entry rejects non-canonical or under-specified profile tiers before execution.
- Split manifest mismatch triggers hard failure by default and only regenerates under explicit user intent.
- Deterministic execution tests cover and pass profile governance plus split mismatch/regeneration behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-deterministic-execution-foundation/01-01-SUMMARY.md`
</output>
