---
phase: 02-fair-dual-mode-hpo-governance
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - survarena/logging/export.py
  - survarena/api/compare.py
  - tests/test_compare_api.py
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03
must_haves:
  truths:
    - "Comparative summaries only use parity-eligible dual-mode rows."
    - "Canonical artifact set includes mode-labeled governance outputs without splitting artifact trees."
    - "Users can inspect requested versus realized budget usage in exported summaries."
  artifacts:
    - path: "survarena/logging/export.py"
      provides: "Parity-gated comparative exports and governance summary rows."
    - path: "survarena/api/compare.py"
      provides: "Dual-mode compare API payloads aligned to benchmark contract."
    - path: "tests/test_compare_api.py"
      provides: "Regression tests for parity gating and governance export contract."
  key_links:
    - from: "survarena/benchmark/runner.py"
      to: "survarena/logging/export.py"
      via: "parity_eligible field in fold/run tables"
      pattern: "parity_eligible"
    - from: "survarena/logging/export.py"
      to: "comparative outputs"
      via: "pre-filtering before pairwise/significance/leaderboard"
      pattern: "pairwise_significance|aggregate_rank_summary|elo_ratings"
---

<objective>
Complete downstream parity enforcement and governance reporting so comparison artifacts are fair, auditable, and canonical.

Purpose: Fulfill D-02 and D-03 by enforcing parity gates at export/API surfaces while keeping one shared artifact set.
Output: Updated export and compare API behavior with passing parity/budget assertions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
@.planning/phases/02-fair-dual-mode-hpo-governance/02-RESEARCH.md
@.planning/phases/02-fair-dual-mode-hpo-governance/02-PATTERNS.md
@survarena/benchmark/runner.py
@survarena/logging/export.py
@survarena/api/compare.py
@tests/test_compare_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply hard parity gate before all comparative summary computations</name>
  <files>survarena/logging/export.py, tests/test_compare_api.py</files>
  <read_first>
    .planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
    .planning/phases/02-fair-dual-mode-hpo-governance/02-RESEARCH.md
    .planning/phases/02-fair-dual-mode-hpo-governance/02-PATTERNS.md
    survarena/logging/export.py
    tests/test_compare_api.py
  </read_first>
  <acceptance_criteria>
    - `rg "parity_eligible" survarena/logging/export.py` returns filtering logic before comparative functions.
    - `rg "pairwise_significance|pairwise_win_rate|elo_ratings|critical_difference_summary" survarena/logging/export.py` shows parity-gated source frame is used.
    - `rg "def test_compare_exports_exclude_parity_ineligible_rows" tests/test_compare_api.py` returns exactly 1 match.
  </acceptance_criteria>
  <action>In export pipelines, compute a parity-gated frame that includes only rows with `parity_eligible=True` before calling comparative aggregators (`aggregate_rank_summary`, `pairwise_win_rate`, `pairwise_significance`, `critical_difference_summary`, and `elo_ratings`) per D-03. Preserve non-eligible rows in raw fold/run ledger exports, but block them from fairness claims.</action>
  <verify>
    <automated>pytest tests/test_compare_api.py -x</automated>
  </verify>
  <done>All comparative outputs are derived strictly from parity-eligible rows; mixed single-mode records no longer affect claims.</done>
</task>

<task type="auto">
  <name>Task 2: Keep canonical artifact set with explicit mode/governance labeling</name>
  <files>survarena/logging/export.py, survarena/api/compare.py</files>
  <read_first>
    .planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
    .planning/phases/02-fair-dual-mode-hpo-governance/02-PATTERNS.md
    docs/protocol.md
    survarena/api/compare.py
    survarena/logging/export.py
  </read_first>
  <acceptance_criteria>
    - `rg "hpo_mode" survarena/api/compare.py survarena/logging/export.py` shows mode label propagation.
    - `rg "requested_max_trials|realized_trial_count|requested_timeout_seconds" survarena/logging/export.py survarena/api/compare.py` shows budget governance field propagation.
    - No new per-mode output root is introduced (`rg "no_hpo/|hpo/" survarena/logging/export.py` returns 0 matches for output-dir branching).
  </acceptance_criteria>
  <action>Ensure compare/export surfaces keep one shared experiment directory and canonical artifact tree per D-02. Use one canonical budget contract across runtime and exports: `requested_max_trials`, `requested_timeout_seconds`, `requested_sampler`, `requested_pruner`, and `realized_trial_count`. Where input rows still carry legacy `trial_count`, map that value to `realized_trial_count` before summary shaping and keep alias passthrough only for backward compatibility. Propagate `hpo_mode`, `parity_key`, and `parity_eligible` into exported tables/summaries so users can audit fairness and budget usage without splitting artifacts into mode-specific directories.</action>
  <verify>
    <automated>pytest tests/test_compare_api.py -x</automated>
  </verify>
  <done>Canonical artifact contract remains single-tree while exposing explicit dual-mode governance fields in outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for parity-gated comparison and budget visibility</name>
  <files>tests/test_compare_api.py</files>
  <read_first>
    .planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
    .planning/phases/02-fair-dual-mode-hpo-governance/02-PATTERNS.md
    survarena/api/compare.py
    survarena/logging/export.py
    tests/test_compare_api.py
  </read_first>
  <acceptance_criteria>
    - `rg "def test_compare_exports_exclude_parity_ineligible_rows" tests/test_compare_api.py` returns exactly 1 match.
    - `rg "def test_compare_summary_includes_requested_vs_realized_budget_fields" tests/test_compare_api.py` returns exactly 1 match.
    - `pytest tests/test_compare_api.py tests/test_dual_mode_hpo_governance.py -x` passes.
  </acceptance_criteria>
  <action>Extend compare API integration tests to validate two outcomes: (1) parity-ineligible rows are excluded from comparative summaries but retained in ledger-style outputs, and (2) summary/export payloads include requested and realized budget fields with concrete names `requested_max_trials`, `requested_timeout_seconds`, and `realized_trial_count` for EXEC-03 auditability.</action>
  <verify>
    <automated>pytest tests/test_compare_api.py tests/test_dual_mode_hpo_governance.py -x</automated>
  </verify>
  <done>Integration tests prove export and compare surfaces enforce parity fairness and budget transparency end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run ledger/fold rows -> summary exports | Integrity-sensitive transition where improper filtering can falsify leaderboard/statistical claims. |
| compare API input -> benchmark/export config | Untrusted API parameters can bypass governance labels if not propagated consistently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | I | `survarena/logging/export.py` | mitigate | Enforce parity-eligible filtering before all comparative statistics and ranking calls. |
| T-02-08 | T | `survarena/api/compare.py` | mitigate | Propagate canonical mode/budget fields from runner outputs without alternate schema branches. |
| T-02-09 | R | exported comparative artifacts | mitigate | Include explicit budget and parity fields so claims remain auditable and non-repudiable. |
</threat_model>

<verification>
Run `pytest tests/test_compare_api.py tests/test_dual_mode_hpo_governance.py -x`, then run `pytest -x` as wave gate to verify no regressions in benchmark/export pathways.
</verification>

<success_criteria>
- Comparative artifacts enforce hard parity gate and never mix single-mode rows into claims.
- Single canonical artifact set is retained with explicit mode and budget governance metadata.
- EXEC-02/EXEC-03 are verifiable from exported outputs and integration tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fair-dual-mode-hpo-governance/02-03-SUMMARY.md`
</output>
