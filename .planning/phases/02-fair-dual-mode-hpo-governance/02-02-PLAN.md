---
phase: 02-fair-dual-mode-hpo-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - survarena/benchmark/tuning.py
  - survarena/benchmark/runner.py
  - tests/test_dual_mode_hpo_governance.py
  - tests/test_hpo_config.py
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03
must_haves:
  truths:
    - "Every selected model executes both no-HPO and HPO paths under the same profile unit."
    - "No-HPO always executes before HPO for each parity unit."
    - "Run records expose requested and realized HPO budget usage for auditability."
  artifacts:
    - path: "survarena/benchmark/runner.py"
      provides: "Dual-pass per-unit orchestration with parity metadata."
    - path: "survarena/benchmark/tuning.py"
      provides: "Uniform budget-policy normalization and realized usage metadata."
  key_links:
    - from: "survarena/benchmark/runner.py"
      to: "survarena/benchmark/tuning.py"
      via: "hpo policy propagation in evaluate_split calls"
      pattern: "hpo_mode|hpo_policy|hpo_metadata"
    - from: "survarena/benchmark/runner.py"
      to: "run payload metrics"
      via: "per-run ledger fields"
      pattern: "requested_max_trials|realized_trial_count|parity_key"
---

<objective>
Implement the runtime dual-mode orchestration and budget governance contract for benchmark execution.

Purpose: Deliver D-01 through D-06 in executable benchmark behavior while preserving deterministic profile controls from Phase 1.
Output: Updated runner/tuning logic with passing dual-mode and budget-governance tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
@.planning/phases/02-fair-dual-mode-hpo-governance/02-RESEARCH.md
@.planning/phases/02-fair-dual-mode-hpo-governance/02-PATTERNS.md
@.planning/phases/01-deterministic-execution-foundation/01-CONTEXT.md
@survarena/benchmark/runner.py
@survarena/benchmark/tuning.py
@tests/test_dual_mode_hpo_governance.py
@tests/test_hpo_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical HPO policy normalization and requested-budget fields</name>
  <files>survarena/benchmark/tuning.py, tests/test_hpo_config.py</files>
  <read_first>
    .planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
    .planning/phases/02-fair-dual-mode-hpo-governance/02-RESEARCH.md
    survarena/benchmark/tuning.py
    tests/test_hpo_config.py
  </read_first>
  <acceptance_criteria>
    - `rg "requested_max_trials|requested_timeout_seconds" survarena/benchmark/tuning.py` returns both field names.
    - `rg "def _parse_hpo_config" survarena/benchmark/tuning.py` confirms parser remains the single normalization path.
    - `pytest tests/test_hpo_config.py -x` passes.
  </acceptance_criteria>
  <action>Update tuning outputs to emit explicit requested policy fields per EXEC-03: `requested_max_trials`, `requested_timeout_seconds`, `requested_sampler`, and `requested_pruner` inside `hpo_metadata`. Define one canonical realized field name as `realized_trial_count`; map existing `hpo_metadata.trial_count` to `realized_trial_count` at metadata construction time, and keep `trial_count` only as a backward-compatible alias during transition. Preserve strict normalization defaults and coercion behavior from existing `_parse_hpo_config()`; do not introduce backend-specific bypasses, per D-05 and D-06.</action>
  <verify>
    <automated>pytest tests/test_hpo_config.py -x</automated>
  </verify>
  <done>Tuning metadata consistently carries normalized requested budget contract and realized usage values for every run mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic no-HPO then HPO execution for each parity unit</name>
  <files>survarena/benchmark/runner.py, tests/test_dual_mode_hpo_governance.py</files>
  <read_first>
    .planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
    .planning/phases/01-deterministic-execution-foundation/01-CONTEXT.md
    survarena/benchmark/runner.py
    survarena/benchmark/tuning.py
    tests/test_dual_mode_hpo_governance.py
  </read_first>
  <acceptance_criteria>
    - `rg "hpo_mode" survarena/benchmark/runner.py` returns mode assignment for both `"no_hpo"` and `"hpo"`.
    - `rg "parity_key|parity_eligible" survarena/benchmark/runner.py` returns both parity fields.
    - `rg "no_hpo.*hpo|hpo.*no_hpo" survarena/benchmark/runner.py` confirms explicit ordering logic is encoded.
    - `pytest tests/test_dual_mode_hpo_governance.py -x` passes.
  </acceptance_criteria>
  <action>Refactor run-loop execution per D-01/D-04 so each dataset/method/split/seed unit executes two deterministic passes in fixed order: first with HPO disabled (`hpo_mode="no_hpo"`), second with HPO enabled (`hpo_mode="hpo"`). Stamp each run payload with `metrics.parity_key` and initialize `metrics.parity_eligible=True`; include the normalized requested budget metadata and canonical realized field `realized_trial_count` in each run payload row (with optional legacy alias passthrough), preserving strict seed and manifest handling per D-06.</action>
  <verify>
    <automated>pytest tests/test_dual_mode_hpo_governance.py -x</automated>
  </verify>
  <done>Runner produces canonical dual-mode run rows with deterministic ordering and auditable budget metadata for every parity unit.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce parity-ineligibility marking at run-ledger production boundary</name>
  <files>survarena/benchmark/runner.py, tests/test_dual_mode_hpo_governance.py</files>
  <read_first>
    .planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
    survarena/benchmark/runner.py
    tests/test_dual_mode_hpo_governance.py
  </read_first>
  <acceptance_criteria>
    - `rg "parity_eligible" survarena/benchmark/runner.py` shows logic that flips the flag when counterpart mode is missing.
    - `rg "comparison_ineligible|missing_counterpart|parity_reason" survarena/benchmark/runner.py` shows explicit reason tagging field.
    - `pytest tests/test_dual_mode_hpo_governance.py -x` passes with missing-mode scenario.
  </acceptance_criteria>
  <action>Implement hard parity gate marker per D-03 at ledger assembly: when exactly one mode exists for a parity key, set `metrics.parity_eligible=False` and include explicit reason text (for example `parity_reason="missing_counterpart_mode"`). Keep raw rows for observability but make downstream comparative pipelines consume the eligibility flag rather than inferring parity ad hoc.</action>
  <verify>
    <automated>pytest tests/test_dual_mode_hpo_governance.py -x</automated>
  </verify>
  <done>Run ledger explicitly marks comparison-ineligible units and preserves complete audit trail without silent row drops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark config -> runner/tuning control flow | Untrusted policy values can alter fairness/budget behavior. |
| runner run rows -> comparative consumers | Integrity boundary where incorrect parity flags can falsify conclusions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `survarena/benchmark/tuning.py` | mitigate | Normalize and stamp requested policy fields centrally; reject implicit backend-specific overrides. |
| T-02-05 | I | `survarena/benchmark/runner.py` | mitigate | Encode deterministic dual-pass order and parity-key labeling; tests must prove no mixed ordering. |
| T-02-06 | D | benchmark HPO runtime | mitigate | Enforce bounded budgets (`max_trials`/`timeout_seconds`) and expose realized usage; high severity if missing. |
</threat_model>

<verification>
Run `pytest tests/test_hpo_config.py tests/test_dual_mode_hpo_governance.py -x` after implementation, then run `pytest tests/test_hpo_config.py tests/test_dual_mode_hpo_governance.py tests/test_compare_api.py -x` to ensure no regression before Wave 3 handoff.
</verification>

<success_criteria>
- Each parity unit emits exactly two ordered mode rows (`no_hpo`, `hpo`) or explicit parity-ineligible marking.
- Requested and realized HPO budget fields are present in run records for governance audit.
- High-severity threats on tampering/integrity/DoS are mitigated by deterministic code paths and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fair-dual-mode-hpo-governance/02-02-SUMMARY.md`
</output>
