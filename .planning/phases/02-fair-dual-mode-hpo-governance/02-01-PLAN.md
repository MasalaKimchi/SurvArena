---
phase: 02-fair-dual-mode-hpo-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_dual_mode_hpo_governance.py
  - tests/test_hpo_config.py
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03
must_haves:
  truths:
    - "For each dataset/split/seed/method unit, users can verify no-HPO and HPO are both represented with explicit mode labels in run/export artifacts."
    - "Users can verify comparison-ineligible units are clearly flagged when one mode is missing, preventing mixed-mode comparative claims."
    - "Users can verify requested-versus-realized HPO budget fields are present in governance-visible outputs."
  artifacts:
    - path: "tests/test_dual_mode_hpo_governance.py"
      provides: "Parity and execution-order contract tests for no-HPO/HPO pairing."
    - path: "tests/test_hpo_config.py"
      provides: "Budget policy normalization and precedence contract tests."
  key_links:
    - from: "tests/test_dual_mode_hpo_governance.py"
      to: "survarena/benchmark/runner.py"
      via: "run_payload and mode-order assertions"
      pattern: "hpo_mode|parity_key|parity_eligible"
    - from: "tests/test_hpo_config.py"
      to: "survarena/benchmark/tuning.py"
      via: "_parse_hpo_config assertions"
      pattern: "max_trials|timeout_seconds|enabled"
---

<objective>
Lock Phase 2 fairness and budget-governance behavior as executable test contracts before runtime code changes.

Purpose: Ensure D-01, D-03, D-04, and D-06 semantics are unambiguous and regression-resistant.
Output: New dual-mode governance test module plus extended HPO config contract tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
@.planning/phases/02-fair-dual-mode-hpo-governance/02-RESEARCH.md
@.planning/phases/02-fair-dual-mode-hpo-governance/02-PATTERNS.md
@tests/test_compare_api.py
@tests/test_benchmark_runner.py
@tests/test_hpo_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author dual-mode pairing and parity-gate contract tests</name>
  <files>tests/test_dual_mode_hpo_governance.py</files>
  <read_first>
    .planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
    .planning/phases/02-fair-dual-mode-hpo-governance/02-PATTERNS.md
    tests/test_compare_api.py
    tests/test_benchmark_runner.py
    tests/test_dual_mode_hpo_governance.py
  </read_first>
  <acceptance_criteria>
    - `rg "def test_run_records_include_hpo_mode_and_parity_key" tests/test_dual_mode_hpo_governance.py` returns exactly 1 match.
    - `rg "def test_dual_mode_execution_order_is_no_hpo_then_hpo" tests/test_dual_mode_hpo_governance.py` returns exactly 1 match.
    - `rg "def test_missing_mode_marks_pairing_unit_ineligible" tests/test_dual_mode_hpo_governance.py` returns exactly 1 match.
    - `rg "hpo_mode|parity_key|parity_eligible" tests/test_dual_mode_hpo_governance.py` returns all 3 marker names.
  </acceptance_criteria>
  <action>Create `tests/test_dual_mode_hpo_governance.py` using monkeypatch-driven runner doubles to assert exact fairness contract fields per D-01/D-03/D-04: each run record must include `metrics.hpo_mode` (`"no_hpo"` or `"hpo"`), deterministic per-unit order `["no_hpo", "hpo"]`, and a parity identifier `metrics.parity_key` built from dataset/split/seed/method context. Add an ineligibility test that verifies missing counterpart mode sets `parity_eligible=False` for comparative pipelines instead of mixing single-mode rows.</action>
  <verify>
    <automated>pytest tests/test_dual_mode_hpo_governance.py -x</automated>
  </verify>
  <done>Dual-mode fairness behavior is fully specified by failing-first tests aligned to D-01, D-03, and D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend HPO config tests for uniform budget policy contract</name>
  <files>tests/test_hpo_config.py</files>
  <read_first>
    .planning/phases/02-fair-dual-mode-hpo-governance/02-CONTEXT.md
    .planning/phases/02-fair-dual-mode-hpo-governance/02-RESEARCH.md
    survarena/benchmark/tuning.py
    tests/test_hpo_config.py
  </read_first>
  <acceptance_criteria>
    - `rg "def test_parse_hpo_config_normalizes_uniform_budget_fields" tests/test_hpo_config.py` returns exactly 1 match.
    - `rg "def test_parse_hpo_config_keeps_trials_and_timeout_together" tests/test_hpo_config.py` returns exactly 1 match.
    - `rg "max_trials|timeout_seconds|sampler|pruner" tests/test_hpo_config.py` confirms all policy keys are asserted.
  </acceptance_criteria>
  <action>Add tests that explicitly encode the benchmark-wide policy contract per EXEC-03: `_parse_hpo_config()` must always emit concrete keys `enabled`, `max_trials`, `timeout_seconds`, `sampler`, `pruner`, and `n_startup_trials`; when both `max_trials` and `timeout_seconds` are configured they must both remain present (no implicit precedence drop), and coercion must reject drift-prone values by clamping to deterministic minima already used in tuning defaults.</action>
  <verify>
    <automated>pytest tests/test_hpo_config.py -x</automated>
  </verify>
  <done>Budget-governance expectations are explicit, testable, and mapped to EXEC-03 before implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark YAML -> tuning policy parser | Untrusted config values cross into execution governance and can tamper fairness. |
| run ledger rows -> comparative summaries | Integrity boundary where missing counterpart modes could create false claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `survarena/benchmark/tuning.py` | mitigate | Encode strict policy-key and coercion expectations in tests before code change; block merge unless tests assert all required fields. |
| T-02-02 | I | `survarena/logging/export.py` comparative inputs | mitigate | Add explicit parity-ineligibility test contract so mixed single-mode rows cannot silently influence comparative outputs. |
| T-02-03 | D | HPO execution budget controls | mitigate | Require test coverage for bounded `max_trials`/`timeout_seconds` contract; high-severity if unbounded behavior remains possible. |
</threat_model>

<verification>
Run `pytest tests/test_dual_mode_hpo_governance.py tests/test_hpo_config.py -x` and confirm failing-first expectations are meaningful (RED phase), then rerun after implementation plans to confirm GREEN.
</verification>

<success_criteria>
- Dual-mode fairness and parity-gate behaviors are defined by automated tests, not prose.
- Uniform budget policy semantics are encoded with exact key-level assertions.
- Threats T-02-01 through T-02-03 have test-level guardrails before runtime modifications begin.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fair-dual-mode-hpo-governance/02-01-SUMMARY.md`
</output>
