---
phase: 02-scientific-comparison-kernel
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - survarena/evaluation/predictions.py
  - survarena/evaluation/metrics.py
  - survarena/benchmark/runner.py
  - survarena/core/results/schema.py
  - tests/test_evaluation.py
  - tests/test_benchmark.py
autonomous: true
requirements: [METR-01, METR-02, METR-03, METR-04, METR-05]
must_haves:
  truths:
    - Invalid risk/survival bundles are rejected before scoring with a machine-readable reason.
    - Requested dataset-level horizons never silently change across folds or due to IPCW clipping.
    - Metric edge fixtures agree with independent references within declared tolerances.
    - Full-distribution metrics and D-calibration are emitted only for capability-valid survival distributions.
  artifacts:
    - path: survarena/evaluation/predictions.py
      provides: Typed fail-closed risk/survival prediction validation.
    - path: survarena/evaluation/metrics.py
      provides: Fixed-horizon support metadata, independently verified metrics, and D-calibration.
    - path: tests/test_evaluation.py
      provides: Adversarial prediction and independent metric-reference fixtures.
  key_links:
    - from: survarena/benchmark/runner.py
      to: survarena/evaluation/predictions.py
      via: validation before compute_survival_metrics
    - from: survarena/benchmark/runner.py
      to: survarena/evaluation/metrics.py
      via: fixed dataset horizons and recorded IPCW support
---

<objective>
Make censoring-aware scoring fail closed, independently checked, fixed-horizon, and capability-aware.
</objective>

<context>
@.planning/phases/02-scientific-comparison-kernel/02-CONTEXT.md
@survarena/evaluation/metrics.py
@survarena/benchmark/runner.py
@survarena/core/models/contract.py
@tests/test_evaluation.py
@tests/test_benchmark.py
</context>

<threat_model>
- **T-03-01 (HIGH, metric integrity):** Transposed, non-monotone, nonfinite, or out-of-range predictions produce plausible metrics. Mitigation: fail-closed bundle validation per D-05.
- **T-03-02 (HIGH, comparability):** Fold-specific or silently clipped horizons change the estimand under the same metric column. Mitigation: fixed requested horizons and unsupported-as-missing per D-04.
- **T-03-03 (HIGH, correctness):** A single production library bug propagates to all claims. Mitigation: independent scikit-survival/SciPy differential fixtures per D-06.
- **T-03-04 (MEDIUM, capability leakage):** Risk-only/fallback methods enter full-distribution metrics. Mitigation: explicit capability/eligibility fields and D-calibration only after full curve validation.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the prediction-bundle validator</name>
  <files>survarena/evaluation/predictions.py, tests/test_evaluation.py</files>
  <read_first>survarena/core/models/contract.py; survarena/evaluation/metrics.py:68-180; survarena/methods/base.py; tests/test_evaluation.py:1-155; 02-CONTEXT.md D-05</read_first>
  <behavior>Risk is 1-D finite length N; survival is N-by-T finite within [0,1], non-increasing per row, and paired to a finite strictly increasing T grid; invalid bundles raise a reason-coded error; risk-only capability is valid only for risk metrics.</behavior>
  <action>Create a small typed validation result/error API with reason codes for row count, dimensionality, finiteness, bounds, grid order/uniqueness, monotonicity tolerance, and capability mismatch. Do not sort, clip, transpose, or otherwise repair invalid model output inside evaluation. Return normalized NumPy arrays only after validation succeeds.</action>
  <acceptance_criteria>Adversarial fixtures cover every reason code; valid constant and decreasing curves pass; tiny numeric monotonicity tolerance is documented/tested; full-distribution request on risk-only capability is rejected.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_evaluation.py -k 'prediction_contract'</automated></verify>
  <done>METR-03 and capability gating have a reusable pre-scoring boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify metrics and fixed support semantics</name>
  <files>survarena/evaluation/metrics.py, tests/test_evaluation.py</files>
  <read_first>survarena/evaluation/metrics.py; tests/test_evaluation.py:1-380; scikit-survival metric APIs installed in the active environment; 02-CONTEXT.md D-04,D-06</read_first>
  <behavior>Harrell/Uno/Brier/IBS/AUC match scikit-survival on deterministic censored fixtures; unsupported horizons remain requested and yield NaN/reason; used support/window fields are recorded; calibration matches an independent optimizer; valid curves emit D-calibration.</behavior>
  <action>Correct IPCW support to follow the censoring-distribution estimability policy rather than maximum event time. Stop clipping requested horizons; compute supported horizon metrics only and attach requested horizon, support upper bound, window bounds, policy, and per-horizon eligibility/reason fields. Add D-calibration for validated full curves with censored-observation redistribution and chi-square output. Add independent differential/golden tests with explicit tolerances and normal/tied/heavy-censoring/support-edge cases.</action>
  <acceptance_criteria>All independent-reference fixtures pass without skip; a horizon above support remains unchanged in provenance and its metric is NaN with `outside_ipcw_support`; calibration/D-calibration are finite only on eligible inputs; per-split outputs expose requested and support fields.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_evaluation.py -k 'reference or ipcw_support or calibration or d_calibration'</automated></verify>
  <done>METR-01, METR-02, and METR-04 are independently verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire fixed horizons and invalid-output evidence into the runner</name>
  <files>survarena/benchmark/runner.py, survarena/core/results/schema.py, tests/test_benchmark.py, tests/test_evaluation.py</files>
  <read_first>survarena/benchmark/runner.py:180-460,1000-1160; survarena/core/results/schema.py; survarena/evaluation/predictions.py; survarena/evaluation/metrics.py; tests/test_benchmark.py:500-850; 02-CONTEXT.md D-04,D-05</read_first>
  <behavior>Dataset-level requested horizons are computed once before run units and reused by every fold/method; invalid outputs create structured ineligible evidence; risk-only methods retain risk metrics but never full-distribution headline eligibility; support metadata enters result rows.</behavior>
  <action>Add requested evaluation horizons to `BenchmarkRunUnit`, derived once from the complete dataset and the versioned protocol quantiles before cells are scheduled. Validate the prediction bundle immediately after adapter prediction. Convert validation/support failures into `comparison_ineligible`, stable `ineligible_reason`, and metric eligibility fields while preserving run/failure evidence. Extend typed result parsing for the new compact scalar provenance fields. Do not regenerate historical matrices.</action>
  <acceptance_criteria>All folds in a dataset carry byte-identical requested horizons; invalid survival output is not scored and has a reason code; risk-only output cannot populate IBS/Brier/AUC/D-calibration headline values; valid CoxPH smoke remains successful and finite for supported metrics.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_benchmark.py tests/test_evaluation.py -k 'horizon or invalid_prediction or capability or metric_support'</automated></verify>
  <done>The live benchmark path enforces and records the Phase 2 metric/prediction contract.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q tests/test_evaluation.py tests/test_benchmark.py`
- `.venv/bin/ruff check survarena/evaluation survarena/benchmark/runner.py survarena/core/results/schema.py tests/test_evaluation.py tests/test_benchmark.py`
- `.venv/bin/python -m mypy survarena/core survarena/benchmark/resume.py survarena/data/splitters.py scripts/audit_manuscript_publishability.py`
</verification>

<success_criteria>
- Every prediction eligibility failure is explicit and machine-readable.
- Every metric has fixed requested-horizon and IPCW-support semantics.
- Independent reference fixtures cover all release metric families.
- The runner cannot silently score invalid/full-distribution-ineligible output.
</success_criteria>

<output>After completion, create `02-03-SUMMARY.md`.</output>
