---
phase: 02-scientific-comparison-kernel
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - survarena/evaluation/_comparison.py
  - survarena/evaluation/_significance.py
  - survarena/evaluation/statistics.py
  - tests/test_evaluation.py
autonomous: true
requirements: [STAT-03, STAT-04, STAT-05, STAT-07]
must_haves:
  truths:
    - Pairwise inference consumes one paired aggregate effect per dataset and reports dataset count as N.
    - Bootstrap point estimates and draws give equal weight to datasets.
    - Friedman/Nemenyi summaries require a complete rectangular dataset-method block and fail closed otherwise.
  artifacts:
    - path: survarena/evaluation/_significance.py
      provides: Dataset-level paired inference, equal-weight uncertainty, and complete-block multiple-method summaries.
    - path: tests/test_evaluation.py
      provides: Pseudoreplication, unequal-fold, all-zero, and incomplete-block regression fixtures.
  key_links:
    - from: survarena/evaluation/_significance.py
      to: survarena/evaluation/_comparison.py
      via: common-support dataset score/effect table
---

<objective>
Make every inferential statistic use independent dataset blocks, explicit support, and valid complete-block assumptions.
</objective>

<context>
@.planning/phases/02-scientific-comparison-kernel/02-CONTEXT.md
@survarena/evaluation/_comparison.py
@survarena/evaluation/_significance.py
@tests/test_evaluation.py
</context>

<threat_model>
- **T-02-01 (HIGH, statistical validity):** Treating folds as independent produces anti-conservative p-values. Mitigation: one paired effect per dataset and explicit `n_datasets` per D-03.
- **T-02-02 (HIGH, assumption violation):** Nemenyi on incomplete blocks gives incomparable ranks/CD. Mitigation: exact rectangular support gate per D-01.
- **T-02-03 (MEDIUM, uncertainty bias):** Concatenating sampled fold arrays weights datasets by fold count. Mitigation: bootstrap dataset aggregates with equal weight.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggregate paired effects before significance testing</name>
  <files>survarena/evaluation/_significance.py, tests/test_evaluation.py</files>
  <read_first>survarena/evaluation/_comparison.py; survarena/evaluation/_significance.py:136-241; tests/test_evaluation.py:495-633; 02-CONTEXT.md D-01,D-02,D-03</read_first>
  <behavior>Fifteen fold rows from one dataset yield n_datasets=1 and no significance claim; multiple datasets yield one delta each; exact-cell pairing precedes aggregation; all-zero differences return p=1 without crashing.</behavior>
  <action>Use exact matched cells to calculate within-dataset paired deltas, then run two-sided Wilcoxon only over dataset aggregates. Report `n_datasets`, `n_matched_cells`, mean/median dataset effect, wins/ties/losses, raw/corrected p, and support policy. Require enough independent datasets for the selected test and return an explicit not-testable reason instead of manufacturing significance.</action>
  <acceptance_criteria>A one-dataset/15-fold fixture reports one inferential unit; row replication within a dataset leaves p/effect unchanged; unmatched cells cannot enter; directed rows mirror effect and wins but share the valid two-sided p-value.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_evaluation.py -k 'pairwise_significance or inferential_unit or pseudoreplication'</automated></verify>
  <done>STAT-03 and exact-pair inference are enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce complete multiple-method blocks</name>
  <files>survarena/evaluation/_significance.py, tests/test_evaluation.py</files>
  <read_first>survarena/evaluation/_comparison.py; survarena/evaluation/_significance.py:244-328; tests/test_evaluation.py:633-680</read_first>
  <behavior>Every included dataset contains every roster method exactly once after aggregation; incomplete blocks raise an actionable error or return explicit ineligibility; average ranks use the same dataset block and K; CD uses true K and N.</behavior>
  <action>Build a rectangular dataset-by-method score matrix from the common-support helper. Validate K>=2, adequate N, no duplicate/missing/nonfinite cells, and identical method roster. Compute ranks and Friedman omnibus on that matrix; only emit Nemenyi CD/post-hoc fields when the omnibus/block policy allows it. Return support/assumption fields with every row.</action>
  <acceptance_criteria>An incomplete three-method block fails closed; a complete block reports exact K/N, ranks 1..K, a K-specific critical value, and a Friedman p-value; adding repeated fold rows does not change K/N.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_evaluation.py -k 'critical_difference or friedman or incomplete_block'</automated></verify>
  <done>STAT-04 and STAT-05 assumptions are executable contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correct equal-weight dataset bootstrap</name>
  <files>survarena/evaluation/_significance.py, survarena/evaluation/statistics.py, tests/test_evaluation.py</files>
  <read_first>survarena/evaluation/_significance.py:1-110; survarena/evaluation/_comparison.py; tests/test_evaluation.py</read_first>
  <behavior>Point mean is the mean of dataset means; a bootstrap draw samples dataset means; unequal fold counts do not change weight; output distinguishes n_datasets from n_cells.</behavior>
  <action>Replace concatenated cluster arrays with a deterministic table of per-dataset aggregates, compute the point estimate from those aggregates, resample dataset rows with replacement, and report mean, median, percentile interval, `n_datasets`, `n_cells`, support policy, and seed. Do not fall back to pooled-fold inference when dataset identity is missing for headline calls.</action>
  <acceptance_criteria>An unequal 1-vs-10-fold fixture has the arithmetic mean of the two dataset means; duplicating folds within either dataset leaves point estimate unchanged; fixed seed reproduces identical interval bounds.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_evaluation.py -k 'bootstrap and dataset'</automated></verify>
  <done>STAT-07 is enforced for point estimates and uncertainty.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q tests/test_evaluation.py`
- `.venv/bin/ruff check survarena/evaluation tests/test_evaluation.py`
</verification>

<success_criteria>
- No cross-dataset p-value uses fold count as sample size.
- Incomplete multiple-method blocks fail closed.
- Dataset weighting is equal in estimates and uncertainty.
</success_criteria>

<output>After completion, create `02-02-SUMMARY.md`.</output>
