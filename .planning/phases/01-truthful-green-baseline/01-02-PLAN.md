---
phase: 01-truthful-green-baseline
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit_manuscript_publishability.py
  - survarena/data/splitters.py
  - tests/test_manuscript_report.py
  - tests/test_benchmark.py
autonomous: true
requirements: [BASE-02, BASE-03]
must_haves:
  truths:
    - The strict release audit reaches a scientific pass/fail verdict using declared dependencies only.
    - Markdown rendering is deterministic and handles pipes, newlines, missing values, and empty frames without tabulate.
    - A split-cache mismatch remains fail-closed and names the precise incompatibility, manifest path, and explicit recovery flag.
    - Explicit regeneration writes a reusable manifest; legacy evidence is never silently migrated.
  artifacts:
    - path: scripts/audit_manuscript_publishability.py
      provides: Dependency-light Markdown rendering and actionable strict report.
    - path: survarena/data/splitters.py
      provides: Stable split-manifest compatibility diff and recovery guidance.
    - path: tests/test_manuscript_report.py
      provides: Exact renderer and strict-audit behavior coverage.
  key_links:
    - from: scripts/audit_manuscript_publishability.py
      to: tests/test_manuscript_report.py
      via: exact escaped Markdown fixtures
    - from: survarena/data/splitters.py
      to: survarena/run_benchmark.py
      via: literal `--regenerate-splits` recovery path
---

<objective>
Make the release audit executable in the declared environment and turn split-cache incompatibility into a safe, actionable diagnostic.

Purpose: Ensure verification failures explain scientific blockers instead of crashing or inviting silent cache mutation.
Output: Internal Markdown renderer, structured mismatch diagnostics, and deterministic regression tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-truthful-green-baseline/01-CONTEXT.md
@.planning/phases/01-truthful-green-baseline/01-RESEARCH.md
@.planning/phases/01-truthful-green-baseline/01-PATTERNS.md
@scripts/audit_manuscript_publishability.py
@survarena/data/splitters.py
@survarena/run_benchmark.py
@tests/test_manuscript_report.py
@tests/test_benchmark.py
</context>

<threat_model>
- **T-02-01 (HIGH, release integrity):** The strict audit can crash in presentation code before reporting scientific blockers. Mitigation: dependency-free renderer plus a subprocess/report verdict test.
- **T-02-02 (HIGH, provenance):** A generic mismatch may encourage deletion or automatic overwrite of a split cache. Mitigation: deterministic field diff and explicit opt-in recovery, retaining fail-closed behavior.
- **T-02-03 (MEDIUM, log injection/readability):** Unescaped table values can corrupt Markdown and obscure the audit. Mitigation: stringify consistently and escape pipes/newlines.
- **T-02-04 (MEDIUM, compatibility):** Missing keys in a legacy manifest could be normalized away and misidentified as current. Mitigation: report missing/added/changed keys and require regeneration to create new identity.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace the optional Markdown renderer</name>
  <files>scripts/audit_manuscript_publishability.py, tests/test_manuscript_report.py</files>
  <read_first>scripts/audit_manuscript_publishability.py; tests/test_manuscript_report.py</read_first>
  <action>Replace `DataFrame.to_markdown` with a small internal renderer for the selected columns. Convert missing values to an explicit stable representation, escape backslashes/pipes, replace CR/LF with HTML line breaks, render a header/separator/body, and preserve `_No rows._` for empty frames. Extend the existing dynamic module loader tests to assert exact output and invoke `build_report()` against controlled empty/temp inputs or run the script so strict mode returns its meaningful blocker status rather than an import exception.</action>
  <acceptance_criteria>No call to `to_markdown` or import of `tabulate` remains; exact tests cover normal, special-character, missing-value, and empty tables; `--strict` emits report content and exits according to audit findings.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_manuscript_report.py</automated></verify>
  <done>The audit renderer cannot prevent the audit from reporting its actual verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Add fail-closed split compatibility diagnostics</name>
  <files>survarena/data/splitters.py, tests/test_benchmark.py</files>
  <read_first>survarena/data/splitters.py:300-470; survarena/run_benchmark.py:1-80; tests/test_benchmark.py:50-125</read_first>
  <action>Add a typed helper that compares observed and expected manifest payload mappings in sorted key order and returns missing, unexpected, and changed field names/values using bounded representations. On mismatch with regeneration disabled, include the manifest path, formatted field differences, and the literal CLI recovery `--regenerate-splits`; never write files in this path. Extend tests to change seeds and omit a fingerprint field, assert diagnostic keys/path/flag, snapshot the existing manifest to prove it is unchanged, then explicitly regenerate and call the loader again to prove exact reuse.</action>
  <acceptance_criteria>Default mismatch errors name at least `seeds`, the concrete `split_manifest.json` path, and `--regenerate-splits`; a legacy missing fingerprint is reported as missing; rejection leaves cache bytes unchanged; opt-in regeneration and subsequent reuse pass.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_benchmark.py -k 'manifest_mismatch'</automated></verify>
  <done>Legacy/incompatible split caches are safe, diagnosable, and deliberately recoverable.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q tests/test_manuscript_report.py tests/test_benchmark.py -k 'manuscript or manifest_mismatch'`
- `.venv/bin/python scripts/audit_manuscript_publishability.py --strict` reaches and prints an audit verdict; an expected nonzero blocker exit is acceptable until Phase 6 evidence exists.
- `.venv/bin/ruff check scripts/audit_manuscript_publishability.py survarena/data/splitters.py tests/test_manuscript_report.py tests/test_benchmark.py`
</verification>

<success_criteria>
- Audit execution has no undeclared rendering dependency.
- Split mismatches expose stable actionable details and do not mutate evidence.
- Explicit regeneration is the only migration path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-green-baseline/01-02-SUMMARY.md`.
</output>
