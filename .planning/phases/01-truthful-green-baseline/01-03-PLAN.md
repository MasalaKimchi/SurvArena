---
phase: 01-truthful-green-baseline
plan: "03"
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/benchmark-smoke.yml
  - README.md
  - PROJECT_STATE.md
  - docs/test_status.md
  - docs/ci_and_reproducibility.md
autonomous: true
requirements: [BASE-01, BASE-04]
must_haves:
  truths:
    - Local documentation and CI execute the same declared lint, incremental type, test, compile, and semantic smoke contracts.
    - Status claims are dated and distinguish locally executed checks, CI definitions, and not-yet-regenerated benchmark evidence.
    - A clean-root CoxPH/WHAS500 one-seed smoke completes and its compact outputs satisfy semantic assertions.
    - Historical result matrices remain explicitly non-citable after behavior-changing fixes.
  artifacts:
    - path: .github/workflows/ci.yml
      provides: Ruff, declared-scope mypy, compile, and full test gates.
    - path: .github/workflows/benchmark-smoke.yml
      provides: Clean benchmark run plus semantic artifact assertions.
    - path: docs/test_status.md
      provides: Dated truthful verification snapshot.
    - path: PROJECT_STATE.md
      provides: Current milestone and evidence validity status.
  key_links:
    - from: README.md
      to: .github/workflows/ci.yml
      via: identical verification commands
    - from: docs/ci_and_reproducibility.md
      to: .github/workflows/benchmark-smoke.yml
      via: exact smoke config and semantic assertions
    - from: PROJECT_STATE.md
      to: .planning/ROADMAP.md
      via: v2 target and remaining blockers
---

<objective>
Consolidate Phase 1 into truthful CI/documentation contracts and prove them through the full suite and a clean semantic benchmark smoke.

Purpose: Turn repaired local logic into a repeatable baseline whose evidence claims a maintainer can audit.
Output: Aligned CI/docs plus recorded green baseline and smoke assertions.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-truthful-green-baseline/01-CONTEXT.md
@.planning/phases/01-truthful-green-baseline/01-VALIDATION.md
@.github/workflows/ci.yml
@.github/workflows/benchmark-smoke.yml
@README.md
@PROJECT_STATE.md
@docs/test_status.md
@docs/ci_and_reproducibility.md
</context>

<threat_model>
- **T-03-01 (HIGH, assurance):** CI/docs can claim checks that were not run or use a different scope than local validation. Mitigation: duplicate no implicit command variants; document the exact incremental mypy targets and executed date.
- **T-03-02 (HIGH, scientific evidence):** A zero exit from the benchmark CLI could mask empty or failed output. Mitigation: smoke workflow asserts row count, success status, finite primary metric, required provenance columns, and compact artifact presence.
- **T-03-03 (HIGH, publication integrity):** Old matrices could be treated as current after logic changes. Mitigation: preserve explicit invalidation and separate code verification from regenerated benchmark evidence.
- **T-03-04 (MEDIUM, supply chain):** A mutable or undeclared CI dependency can diverge from local gates. Mitigation: install the project development extra and use versions pinned/ranged in `pyproject.toml`; full locking remains Phase 6.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Align CI with the declared baseline and semantic smoke</name>
  <files>.github/workflows/ci.yml, .github/workflows/benchmark-smoke.yml</files>
  <read_first>pyproject.toml; .github/workflows/ci.yml; .github/workflows/benchmark-smoke.yml; scripts/validate_benchmark_protocol.sh</read_first>
  <action>Add a fast mypy job or step using exactly the Plan 01 scope and the development-extra version; include `python -m compileall -q survarena` in the main verification path. In benchmark smoke, run the one-seed WHAS500/CoxPH slice into a clean temporary output directory and add a Python assertion step that discovers the fold result, verifies at least one successful row, finite `uno_c`, required identity columns including `hpo_mode`, and the expected compact artifact set. Keep foundation extras out of baseline CI and do not present workflow definition as proof it has run remotely.</action>
  <acceptance_criteria>Workflow YAML parses; CI and README commands match; the smoke fails for empty results, all-failed rows, non-finite primary metrics, missing arm identity, or missing compact artifacts.</acceptance_criteria>
  <verify><automated>.venv/bin/python -c "import pathlib, yaml; [yaml.safe_load(p.read_text()) for p in pathlib.Path('.github/workflows').glob('*.yml')]" &amp;&amp; rg -n "mypy|compileall|uno_c|hpo_mode" .github/workflows/ci.yml .github/workflows/benchmark-smoke.yml</automated></verify>
  <done>Automation represents behavior, type, syntax, and semantic benchmark validity rather than exit codes alone.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stale verification and evidence claims</name>
  <files>README.md, PROJECT_STATE.md, docs/test_status.md, docs/ci_and_reproducibility.md</files>
  <read_first>README.md:270-320; PROJECT_STATE.md; docs/test_status.md; docs/ci_and_reproducibility.md; .planning/PROJECT.md; .planning/ROADMAP.md</read_first>
  <action>Document the exact Phase 1 commands, the incremental mypy scope, and how to interpret an expected strict-audit blocker exit. Rewrite the status pages around three explicit categories: locally verified on 2026-08-31, configured but not independently observed in hosted CI, and benchmark evidence requiring v2 regeneration. Name `v2.0 Verified Benchmark Kernel` and target `survbench-1.0-rc1`, link the roadmap, retain historical evidence invalidation, and remove constrained-environment claims that no longer describe this workspace.</action>
  <acceptance_criteria>All four files contain the exact commands/scopes and date; none claims historical no-HPO/HPO matrices are current; CI definitions are not described as completed remote runs; remaining scientific/execution/reproduction phases are visible.</acceptance_criteria>
  <verify><automated>rg -n "2026-08-31|mypy|survbench-1.0-rc1|invalid|regenerat" README.md PROJECT_STATE.md docs/test_status.md docs/ci_and_reproducibility.md</automated></verify>
  <done>A maintainer can distinguish code health from release evidence and remaining roadmap work.</done>
</task>

<task type="auto">
  <name>Task 3: Execute and record the integrated baseline</name>
  <files>PROJECT_STATE.md, docs/test_status.md</files>
  <read_first>.planning/phases/01-truthful-green-baseline/01-VALIDATION.md; configs/benchmark/standard_v1.yaml; scripts/validate_benchmark_protocol.sh; PROJECT_STATE.md; docs/test_status.md</read_first>
  <action>Run Ruff, the declared mypy scope, full pytest, compileall, and the strict audit. Run CoxPH/WHAS500 with one seed into a newly created `/tmp` directory so no repository evidence is mixed in; assert successful finite results and required compact outputs. Record exact pass counts/commands and the audit's scientific blocker verdict in the status docs. Do not copy the smoke artifacts into `results/` or claim manuscript readiness.</action>
  <acceptance_criteria>Ruff, mypy, pytest, compileall, and semantic smoke exit zero; strict audit completes without exception and its nonzero status, if any, corresponds only to named release-evidence blockers; docs record actual observed counts rather than planned values.</acceptance_criteria>
  <verify><automated>.venv/bin/ruff check survarena tests scripts &amp;&amp; .venv/bin/python -m mypy survarena/core survarena/benchmark/resume.py survarena/data/splitters.py scripts/audit_manuscript_publishability.py &amp;&amp; .venv/bin/python -m pytest -q &amp;&amp; .venv/bin/python -m compileall -q survarena</automated></verify>
  <done>The repository has a truthful green development baseline and a verified core benchmark smoke, while publication evidence remains correctly blocked.</done>
</task>

</tasks>

<verification>
- `ruff check survarena tests scripts`
- `.venv/bin/python -m mypy survarena/core survarena/benchmark/resume.py survarena/data/splitters.py scripts/audit_manuscript_publishability.py`
- `.venv/bin/python -m pytest -q`
- `.venv/bin/python -m compileall -q survarena`
- `.venv/bin/python scripts/audit_manuscript_publishability.py --strict` produces an actionable verdict without traceback.
- Clean-root one-seed CoxPH/WHAS500 run satisfies the workflow's semantic assertions.
</verification>

<success_criteria>
- BASE-01 and BASE-04 are supported by executed evidence.
- CI, README, and status docs agree on commands and scope.
- No generated benchmark result is added to the repository.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-green-baseline/01-03-SUMMARY.md`.
</output>
