---
phase: 01-truthful-green-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - survarena/benchmark/runner.py
  - survarena/benchmark/resume.py
  - survarena/core/results/store.py
  - tests/test_benchmark.py
  - tests/test_core_results.py
autonomous: true
requirements: [BASE-01]
must_haves:
  truths:
    - The four known failures become deterministic assertions of the intended contracts, not skips or weakened checks.
    - Artifact persistence failure leaves usable prediction evidence and a structured artifact failure while benchmark scoring remains successful.
    - Legacy resume evidence without an arm can complete no_hpo only and never suppress an HPO run.
    - Process isolation remains in production and Parquet behavior is verified with and without pyarrow.
    - The declared mypy scope passes and is explicitly narrower than the full package.
  artifacts:
    - path: pyproject.toml
      provides: Pinned mypy development dependency and canonical incremental type configuration.
    - path: tests/test_benchmark.py
      provides: Artifact, legacy resume, and process-scheduler regression coverage.
    - path: tests/test_core_results.py
      provides: Deterministic Parquet available/unavailable branch coverage.
  key_links:
    - from: tests/test_benchmark.py
      to: survarena/benchmark/runner.py
      via: artifact and `_execute_run_units` contracts
    - from: tests/test_benchmark.py
      to: survarena/benchmark/resume.py
      via: five-field comparison-arm completion identity
    - from: tests/test_core_results.py
      to: survarena/core/results/store.py
      via: lazy pyarrow import boundary
---

<objective>
Restore deterministic verification of the benchmark's artifact, resume, process execution, and Parquet contracts, then establish a real incremental static-type gate.

Purpose: Remove false-red and false-green test behavior without changing the scientific protocol.
Output: Four repaired regression contracts, mypy configuration, and a green targeted baseline.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-truthful-green-baseline/01-CONTEXT.md
@.planning/phases/01-truthful-green-baseline/01-RESEARCH.md
@.planning/phases/01-truthful-green-baseline/01-PATTERNS.md
@survarena/benchmark/runner.py
@survarena/benchmark/resume.py
@survarena/core/results/store.py
@tests/test_benchmark.py
@tests/test_core_results.py
@pyproject.toml
</context>

<threat_model>
- **T-01-01 (HIGH, evidence integrity):** Making the suite green by skipping installed-dependency paths or accepting a run-fatal artifact failure could hide lost benchmark evidence. Mitigation: assert predictions, manifest status, and run-level success explicitly.
- **T-01-02 (HIGH, reproducibility):** Replacing process execution with threads, or testing process workers through parent memory, could mask RNG races. Mitigation: retain `ProcessPoolExecutor` and test its boundary with a synchronous fake.
- **T-01-03 (MEDIUM, provenance):** A legacy arm-less row could suppress an HPO cell that never ran. Mitigation: assert both comparison arms and exact call count/mode.
- **T-01-04 (MEDIUM, assurance):** A nominal type gate could pass by excluding all meaningful code. Mitigation: declare the concrete module scope in `pyproject.toml`, cover `survarena/core` plus Phase 1 kernel modules, and avoid blanket error-code suppression.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Align artifact and resume regression contracts</name>
  <files>survarena/benchmark/runner.py, survarena/benchmark/resume.py, tests/test_benchmark.py</files>
  <read_first>survarena/benchmark/runner.py:350-410,596-690; survarena/benchmark/resume.py; tests/test_benchmark.py:680-920</read_first>
  <action>Preserve the current non-fatal artifact policy: predictions are saved first, an unpickleable fitted state yields `model_artifact_status="failed"`, no model path, a populated error, and no exception that invalidates already-computed metrics. Update the stale direct helper test to pass `hpo_mode="no_hpo"`, assert the arm-qualified path and manifest fields, and add/strengthen an `evaluate_split` assertion that artifact failure keeps run status successful. For resume, keep the five-field key and add a dual-arm legacy fixture proving an arm-less successful row skips only `no_hpo` while exactly one `hpo` unit executes. Do not map a missing arm to every configured mode.</action>
  <acceptance_criteria>`test_model_artifact_request_fails_if_fitted_state_is_not_pickleable` is renamed to reflect structured non-fatal failure; its manifest contains `model_artifact_status == "failed"`, an existing prediction path, no model path, and an error. The legacy resume test configures both modes and observes exactly one HPO evaluation, while an explicit matching arm suppresses only that arm.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_benchmark.py -k 'artifact or resume'</automated></verify>
  <done>Artifact and resume behavior are explicit, comparison-arm-safe, and green without altering scientific results.</done>
</task>

<task type="auto">
  <name>Task 2: Test the process scheduler at the correct boundary</name>
  <files>survarena/benchmark/runner.py, tests/test_benchmark.py</files>
  <read_first>survarena/benchmark/runner.py:940-970,1050-1170; tests/test_benchmark.py:1080-1160</read_first>
  <action>Keep `ProcessPoolExecutor` as the production scheduler. Change the configured-worker regression test to patch `runner.ProcessPoolExecutor` with a context-manager fake whose `map` evaluates the supplied units synchronously and records `max_workers`, the mapped function, and unit count. Assert two results and `max_workers == 2` without depending on state mutations in a child. Strengthen the serial test so neither process nor thread execution is constructed at `n_jobs=1`.</action>
  <acceptance_criteria>The parallel test fails if production selects `ThreadPoolExecutor`, ignores `n_jobs`, or maps the wrong number of units; the serial test fails if any executor is opened.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_benchmark.py -k 'execution_defaults_to_serial or execution_uses_configured_n_jobs or execution_rejects_invalid_n_jobs'</automated></verify>
  <done>The scheduler contract is deterministic while process-level RNG isolation remains intact.</done>
</task>

<task type="auto">
  <name>Task 3: Make Parquet branches and the type gate environment-independent</name>
  <files>pyproject.toml, survarena/core/results/store.py, tests/test_core_results.py</files>
  <read_first>pyproject.toml; survarena/core/results/store.py:390-435; tests/test_core_results.py:280-325</read_first>
  <action>Split the file-backed store round-trip from Parquet capability tests. When pyarrow is installed, export all canonical tables to a temporary directory and assert readable Parquet files/expected rows. Force the unavailable branch by patching the import boundary (prefer a narrow `_require_pyarrow`/capability helper if needed) and assert the actionable RuntimeError without uninstalling dependencies. Add `mypy==2.3.1` to the dev extra and `[tool.mypy]` with Python 3.10, explicit package bases, `check_untyped_defs`, `no_implicit_optional`, and deliberate third-party missing-import handling. Declare the initial scope as `survarena/core`, `survarena/benchmark/resume.py`, `survarena/core/results/store.py`, `survarena/data/splitters.py`, and `scripts/audit_manuscript_publishability.py`; fix real errors in that scope without blanket per-file ignores.</action>
  <acceptance_criteria>Both Parquet branches execute deterministically; generated Parquet can be read back; the development extra contains exactly pinned mypy 2.3.1; the declared-scope mypy command exits zero.</acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_core_results.py &amp;&amp; .venv/bin/python -m mypy survarena/core survarena/benchmark/resume.py survarena/data/splitters.py scripts/audit_manuscript_publishability.py</automated></verify>
  <done>Result export coverage no longer depends on the workstation environment and a meaningful incremental type gate exists.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q tests/test_benchmark.py tests/test_core_results.py`
- `.venv/bin/python -m mypy survarena/core survarena/benchmark/resume.py survarena/data/splitters.py scripts/audit_manuscript_publishability.py`
- `.venv/bin/ruff check survarena/benchmark/runner.py survarena/benchmark/resume.py survarena/core/results tests/test_benchmark.py tests/test_core_results.py`
</verification>

<success_criteria>
- The original four failing nodes pass for contract-based reasons.
- No test conditionally skips the available or unavailable Parquet behavior.
- Production parallelism remains process-based.
- The initial type scope is declared and green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-green-baseline/01-01-SUMMARY.md`.
</output>
