---
quick_id: 260901-0gy
phase: quick-260901-0gy
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - pyproject.toml
  - .python-version
  - uv.lock
  - requirements.txt
  - docs/conf.py
  - docs/index.md
  - .gitignore
  - README.md
  - docs/environment.md
  - .github/workflows/ci.yml
  - .github/workflows/benchmark-smoke.yml
  - tests/test_developer_infrastructure.py
  - docs/ci_and_reproducibility.md

must_haves:
  truths:
    - "A contributor can create the preferred Python 3.11 environment from the committed uv lock and run the documented Ruff, mypy, pytest, compile, and Sphinx commands without relying on host-installed tool versions."
    - "A stale pyproject/lock combination fails locally and in pull-request CI instead of being silently re-resolved."
    - "Every tracked Markdown guide is reachable from a Sphinx toctree, and the HTML build fails on warnings or unresolved cross-references."
    - "Pull requests to main automatically run lock validation, Ruff, scoped mypy, the lightweight import matrix, full pytest on the supported-version boundaries, compileall, and the strict documentation build."
    - "The committed uv.lock is described truthfully as the developer/CI lock; it is not presented as the canonical Linux/amd64 manuscript-release environment reserved by the project roadmap."
  artifacts:
    - path: "pyproject.toml"
      provides: "uv version policy, PEP 735 tool groups, and strict pytest defaults"
      contains: "[dependency-groups]"
    - path: ".python-version"
      provides: "Preferred local CPython version"
      contains: "3.11"
    - path: "uv.lock"
      provides: "Committed, generated, cross-platform dependency resolution for project extras and local groups"
    - path: "docs/conf.py"
      provides: "Sphinx/MyST configuration with nitpicky warning enforcement and a plain-text Mermaid lexer fallback"
      contains: "TextLexer"
    - path: "docs/index.md"
      provides: "Root documentation page and complete toctree"
      contains: "{toctree}"
    - path: ".github/workflows/ci.yml"
      provides: "Read-only, cached, uv-backed PR quality gates"
      contains: "uv sync --locked"
    - path: "tests/test_developer_infrastructure.py"
      provides: "Regression assertions for dependency groups, Python support, pytest strictness, workflow triggers, matrices, permissions, credential-safe checkout, profile cache suffixes, locked commands, and immutable action pins"
    - path: "docs/ci_and_reproducibility.md"
      provides: "Exact local/CI contract and reproducibility boundary"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock --check and uv sync --locked"
      pattern: "uv (lock --check|sync --locked)"
    - from: ".python-version"
      to: ".github/workflows/ci.yml"
      via: "setup-uv's explicit Python selection, with 3.11 as the non-matrix default"
      pattern: "python-version"
    - from: "docs/index.md"
      to: "docs/conf.py"
      via: "sphinx-build reads the MyST root document and its toctree"
      pattern: "sphinx-build.*-W"
    - from: "docs/benchmarking_workflow.md"
      to: "docs/conf.py"
      via: "the existing Mermaid fence resolves through a registered plain-text Pygments fallback"
      pattern: "lexers.*mermaid.*TextLexer"
    - from: ".github/workflows/ci.yml"
      to: "uv.lock"
      via: "setup-uv cache key plus locked group syncs"
      pattern: "cache-dependency-glob:.*uv\\.lock"
    - from: ".github/workflows/ci.yml"
      to: "docs/conf.py"
      via: "strict Sphinx PR job"
      pattern: "sphinx-build.*(-n|--nitpicky).*(-W|--fail-on-warning)"
---

<objective>
Adopt uv as SurvArena's reproducible developer/CI environment path, turn the existing Markdown guide set into a strict Sphinx site, and make pull requests enforce the same locked Ruff, mypy, pytest, compile, and documentation checks contributors run locally.

Purpose: Replace host-tool and pip-resolution drift with one explicit developer contract while preserving SurvArena's supported Python range, heavy-runtime CI budget, and citable-release boundary.

Output: A committed uv lock and Python pin, Sphinx/MyST configuration and navigation, uv-backed GitHub Actions, and synchronized contributor/reproducibility documentation.
</objective>

<execution_context>
@/Users/justin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/justin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@AGENTS.md
@pyproject.toml
@requirements.txt
@README.md
@docs/index.md
@docs/environment.md
@docs/ci_and_reproducibility.md
@.github/workflows/ci.yml
@.github/workflows/benchmark-smoke.yml

<current_contracts>
- `pyproject.toml` is already the dependency source of truth; runtime and foundation/tracking extras must retain their existing semantics and pins.
- CPython 3.10, 3.11, and 3.12 are supported; 3.11 is preferred.
- Ruff covers `survarena tests scripts` at Python 3.10 syntax and line length 120.
- Mypy intentionally covers only `survarena/core`, `survarena/benchmark/resume.py`, `survarena/data/splitters.py`, and `scripts/audit_manuscript_publishability.py` with imported legacy modules skipped.
- The benchmark smoke is manual-only and must stay separate from PR CI.
- The working tree contains unrelated tracked and untracked work, including application source, tests, manuscript documentation, and container scaffolding. Modify and stage only `files_modified`; do not alter `Dockerfile`, `.dockerignore`, `docs/manuscript_publishability.md`, `docs/code_quality_and_efficiency.md`, application source, or existing test changes.
</current_contracts>

<dirty_worktree_protocol>
- Before editing, record `BASE_SHA=$(git rev-parse HEAD)` and create a manifest under a `mktemp -d` directory containing the porcelain status plus SHA-256 hashes for every pre-existing dirty file, recursively hashing files below dirty untracked directories. The manifest is evidence only and must stay outside the repository.
- Never clean, stash, reset, restore, move, or rewrite pre-existing dirty paths. Adding `tests/test_developer_infrastructure.py` is allowed; altering any existing dirty test file is not.
- Run each pre-commit task verifier in a clean detached candidate worktree made from the current `HEAD`, overlaying only that task's exact `<files>` from the original checkout while preserving their relative paths. Prior task commits are therefore present, the current task's uncommitted files are testable, and unrelated tracked/untracked work is absent. Never copy a whole dirty directory into the candidate worktree; remove only the exact temporary candidate worktree after its verifier finishes.
- After implementation commits, require `git diff --name-only "$BASE_SHA..HEAD"` to be a subset of this plan's `files_modified` plus GSD quick-task artifacts, then regenerate the dirty-file manifest in the original checkout and compare it byte-for-byte with the baseline manifest.
- Run the final lock/lint/type/test/docs gates in a detached validation worktree created from the post-task `HEAD` under a new `mktemp -d` path. This ensures untracked `docs/code_quality_and_efficiency.md` and all other unrelated local changes cannot affect Sphinx, Ruff, mypy, or pytest. Remove only that exact temporary worktree after validation; do not encode a permanent Sphinx exclusion for the unrelated document.
</dirty_worktree_protocol>

<scope_guardrails>
- Do not add documentation deployment, release publishing, Dependabot, pre-commit, coverage thresholds, or scheduled network link checking.
- Do not enable `ruff format --check` or expand Ruff rule selection in this task. The repository currently has 44 formatter-dirty files, so a format gate would require unrelated source churn.
- Do not add Sphinx autodoc or dependency mocks. This task makes the maintained narrative Markdown buildable without importing SurvArena's heavy ML dependency graph.
- Preserve the existing pip `dev` extra and `requirements.txt` only as clearly labeled compatibility paths; uv plus `uv.lock` becomes the primary contributor and CI path.
- Keep the listed 13-file scope together: dependency metadata/lock, local documentation, workflows, and their regression test are one executable local/CI contract. This carries elevated integration risk, so preserve task order and require the final detached-worktree equivalence gate rather than splitting ownership across quick tasks.
</scope_guardrails>
</context>

<package_legitimacy_audit>

## Package Legitimacy Audit

| Package/tool | Status | Evidence | Planned use |
|---|---|---|---|
| `uv==0.12.8` | VERIFIED | [Official versioned-installer guidance](https://docs.astral.sh/uv/getting-started/installation/), [project/lock docs](https://docs.astral.sh/uv/concepts/projects/sync/), and [GitHub Actions guide](https://docs.astral.sh/uv/guides/integration/github/) | Bootstrap an isolated exact binary, generate/check `uv.lock`, sync exact groups, and run local/CI commands |
| `Sphinx>=8.2.3,<9` | VERIFIED | [Official Sphinx documentation](https://www.sphinx-doc.org/en/master/) and [official PyPI release](https://pypi.org/project/Sphinx/8.2.3/) | Build documentation on the preferred Python 3.11 line without importing application modules |
| `myst-parser>=5.1,<6` | VERIFIED | [Sphinx's official Markdown guidance](https://www.sphinx-doc.org/en/master/usage/markdown.html) and [official PyPI project](https://pypi.org/project/myst-parser/) | Parse the repository's existing Markdown sources in Sphinx |
| `tomli>=2.4.1,<3; python_version < "3.11"` | VERIFIED | [Official PyPI project and 2.4.1 release metadata](https://pypi.org/project/tomli/2.4.1/) documents the maintained `tomllib` backport and Python >=3.8 support | Let the infrastructure regression test parse `pyproject.toml` on the supported Python 3.10 lane only |
| `astral-sh/setup-uv` v9.0.0 | VERIFIED | [Official uv Actions guide](https://docs.astral.sh/uv/guides/integration/github/) pins commit `c771a70e6277c0a99b617c7a806ffedaca235ff9` | Install pinned uv/Python and persist uv's cache |
| `actions/checkout` v7.0.1 | VERIFIED | [Official uv Actions example](https://docs.astral.sh/uv/guides/integration/github/) pins commit `3d3c42e5aac5ba805825da76410c181273ba90b1` | Read-only source checkout |
| `actions/upload-artifact` v7.0.1 | VERIFIED | [Official action release](https://github.com/actions/upload-artifact/releases/tag/v7.0.1) at commit `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` | Preserve manual benchmark-smoke diagnostics |

All introduced packages and actions are VERIFIED. There are no `[ASSUMED]`, `[SUS]`, or `[SLOP]` entries, so no package-legitimacy checkpoint is required.
</package_legitimacy_audit>

<uv_bootstrap>
Before changing `pyproject.toml`, create `UV_BOOTSTRAP_ROOT` with `mktemp -d`, download the official versioned installer from `https://astral.sh/uv/0.12.8/install.sh` into that directory, and execute it with `UV_INSTALL_DIR="$UV_BOOTSTRAP_ROOT/bin"` and `UV_NO_MODIFY_PATH=1`. Set `UV_BIN` to the resulting absolute `.../bin/uv` path and verify it prints `uv 0.12.8`. Use that exact `UV_BIN` for every local lock/sync/run command and every candidate/final verifier in this plan; if commands run in separate shells, substitute the retained absolute path rather than assuming an exported variable. Keep the isolated bootstrap directory until final validation finishes, then remove only that exact temporary directory. Do not run `uv self update`, replace the user's global uv 0.11.29, or depend on the global binary after adding `required-version`.
</uv_bootstrap>

<tasks>

<task type="auto">
  <name>Task 1: Define the uv and pytest project contract</name>
  <files>pyproject.toml, .python-version, uv.lock, requirements.txt</files>
  <action>
Pin the preferred interpreter in `.python-version` to `3.11` and align published metadata with the repository's documented support contract by changing `requires-python` to `>=3.10,<3.13`. In `pyproject.toml`, set `[tool.uv].required-version = "==0.12.8"` and add PEP 735 groups with non-overlapping responsibilities: `quality` contains the existing Ruff and mypy constraints; `test` contains the existing pytest constraint plus `tomli>=2.4.1,<3; python_version < '3.11'` solely as the Python 3.10 backport used by the infrastructure test; `import-smoke` repeats only the six exact lightweight runtime pins needed by the current lazy-import job (`numpy`, `pandas`, `scipy`, `scikit-learn`, `PyYAML`, `pyarrow`); `docs` contains `Sphinx>=8.2.3,<9` and `myst-parser>=5.1,<6`; and `dev` uses PEP 735 `include-group` entries for `quality` plus `test`. Declare the `docs` group as requiring Python `>=3.11` under uv's dependency-group metadata. Keep all runtime dependencies and foundation/tracking extras unchanged. Keep the existing `[project.optional-dependencies].dev` entries with identical tool constraints plus the same conditional Tomli backport as the pip-facing compatibility extra, and update `requirements.txt` comments to direct contributors to uv/`uv.lock` first without changing its compatibility install target.

Add pytest 8 strict defaults under `[tool.pytest.ini_options]`: `-ra`, `--strict-config`, `--strict-markers`, and `xfail_strict = true`; do not introduce blanket warning suppression. Generate and commit `uv.lock` with uv 0.12.8. The lock must resolve the complete project metadata, optional extras, and dependency groups across the declared Python range; do not hand-edit it, relax application dependency pins, or remove optional backends to force resolution. Document in comments only where needed that `uv.lock` is the developer/CI resolution, not the Phase 6 canonical release image/lock.
  </action>
  <verify>
    <automated>test -x "$UV_BIN" &amp;&amp; "$UV_BIN" --version | grep -F "uv 0.12.8" &amp;&amp; "$UV_BIN" lock --check &amp;&amp; "$UV_BIN" sync --locked --only-group quality --python 3.11 &amp;&amp; "$UV_BIN" run --no-sync ruff --version &amp;&amp; "$UV_BIN" run --no-sync python -m mypy --version &amp;&amp; "$UV_BIN" sync --locked --no-default-groups --group test --python 3.11 &amp;&amp; "$UV_BIN" run --no-sync python -m pytest --collect-only -q &amp;&amp; "$UV_BIN" run --no-sync python -c "import pathlib, tomllib; data = tomllib.loads(pathlib.Path('pyproject.toml').read_text()); opts = data['tool']['pytest']['ini_options']; assert data['project']['requires-python'] == '&gt;=3.10,&lt;3.13'; assert {'-ra', '--strict-config', '--strict-markers'} &lt;= set(opts['addopts']); assert opts['xfail_strict'] is True"</automated>
  </verify>
  <done>`uv.lock` is generated and current, `.python-version` selects 3.11, the light quality group installs without SurvArena's ML runtime, pytest rejects unknown configuration/markers, and existing runtime/extras plus the pip compatibility path retain their behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Build the existing Markdown guides as strict Sphinx documentation</name>
  <files>docs/conf.py, docs/index.md, .gitignore, README.md, docs/environment.md</files>
  <action>
Create `docs/conf.py` for project `SurvArena` with `myst_parser`, `.md` as Markdown, `index` as the root document, `nitpicky = True`, heading anchors deep enough for the existing section-fragment links, and Sphinx's bundled theme so no theme package is added. Import `TextLexer` from `pygments.lexers.special` and register `sphinx.highlighting.lexers["mermaid"] = TextLexer()` so the existing fenced Mermaid source in `docs/benchmarking_workflow.md` stays readable as plain text and cannot emit an unknown-Pygments-lexer warning under `-W`; do not add a Mermaid package or rewrite the fence. Exclude only generated Sphinx directories and standard OS/editor artifacts; do not suppress warning classes broadly. Keep autodoc disabled because the current guide set is narrative and application imports pull in the heavy ML stack.

Preserve the useful routing tables in `docs/index.md` and add explicit MyST toctrees covering every tracked Markdown guide exactly once, organized into user guides, benchmark/reference material, evidence/status material, and contributor guides. Add `docs/_build/` to `.gitignore`. Make uv the first installation/development path in `README.md` and `docs/environment.md`: install the pinned uv release, run `uv sync --locked`, use `uv run --no-sync` after an explicit sync, include optional foundation extras with `uv sync --locked --extra ...`, check lock freshness with `uv lock --check`, and build docs with the exact strict command below. Keep `scripts/setup_env.sh` and pip instructions visibly labeled as compatibility paths, not lock-equivalent workflows. For an intentional single-package update, require contributors to edit the intended constraint in `pyproject.toml` first (especially exact runtime pins), then run `uv lock --upgrade-package NAME`, review the lock diff, and finish with `uv lock --check`; do not imply the upgrade command overrides an unchanged exact pin. State that citable benchmark evidence still requires the separately governed Phase 6 Linux/amd64 environment.
  </action>
  <verify>
    <automated>test -x "$UV_BIN" &amp;&amp; "$UV_BIN" sync --locked --only-group docs --python 3.11 &amp;&amp; "$UV_BIN" run --no-sync python -c "import runpy; from pygments.lexers.special import TextLexer; from sphinx.highlighting import lexers; runpy.run_path('docs/conf.py'); assert isinstance(lexers.get('mermaid'), TextLexer)" &amp;&amp; "$UV_BIN" run --no-sync sphinx-build -n -W --keep-going -b html docs docs/_build/html &amp;&amp; test -f docs/_build/html/index.html</automated>
  </verify>
  <done>All tracked Markdown guides are navigable from `docs/index.md`, the existing Mermaid fence uses the registered plain-text lexer without a Pygments warning, the generated HTML index exists, missing documents/references and all other Sphinx warnings fail the build, generated HTML stays untracked, and local setup/lock/docs commands are copy-pasteable.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce the locked local contract in GitHub Actions</name>
  <files>.github/workflows/ci.yml, .github/workflows/benchmark-smoke.yml, tests/test_developer_infrastructure.py, docs/ci_and_reproducibility.md</files>
  <action>
Refactor `.github/workflows/ci.yml` without weakening its existing triggers, least-privilege `contents: read`, concurrency cancellation, timeouts, or failure visibility. Pin every action to the audited full commit SHA with its release comment, and set `persist-credentials: false` on every `actions/checkout` step in both workflows. Use `astral-sh/setup-uv` v9.0.0 with uv 0.12.8, explicit Python selection, `enable-cache: true`, and `uv.lock` as the cache dependency. Give every setup-uv step a stable `cache-suffix` based on the dependency profile rather than the job/display name: `quality` for both lock/Ruff and scoped-mypy jobs, `docs` for Sphinx, `import-smoke` for every import-matrix lane, `full-tests` for every pytest/compile lane, and `benchmark-smoke` for the manual benchmark. Every dependency install must use `uv sync --locked`, and every subsequent command must use `uv run --no-sync` so CI cannot mutate the lock or silently resync.

Keep stable, descriptive jobs for: (1) lock freshness plus `ruff check --output-format=github survarena tests scripts`; (2) the existing scoped mypy command; (3) lazy top-level import with only the locked `import-smoke` group on Python 3.10, 3.11, and 3.12; (4) full `pytest -q` plus `compileall` on Python 3.10 and 3.12, covering the minimum and maximum supported versions while the 3.11 default is exercised by quality/docs/manual smoke; and (5) strict Sphinx HTML on Python 3.11 using `-n -W --keep-going`. Use `--only-group` for quality/import/docs jobs and `--no-default-groups --group test` for full tests so cheap gates do not install the heavy application stack unnecessarily. Do not add Ruff formatting, package publishing, docs deployment, or secrets to PR jobs.

Apply the same pinned checkout/setup-uv/cache/locked-sync pattern to the manual-only benchmark smoke, run the benchmark and assertions through `uv run --no-sync`, update `actions/upload-artifact` to its audited SHA, and keep the trigger manual-only. Update `docs/ci_and_reproducibility.md` with exact job/Python contracts, local equivalents, cache/lock behavior, and the honest distinction between the cross-platform developer/CI `uv.lock` and the canonical release environment. Do not claim a hosted run occurred during this local change.

Create `tests/test_developer_infrastructure.py` as the executable regression contract for this infrastructure. Import stdlib `tomllib` when available and fall back to the audited `tomli` backport on Python 3.10; parse both workflows with `yaml.load(..., Loader=yaml.BaseLoader)` so YAML's `on` key is not coerced to a boolean. Assert the exact `requires-python` bound, uv required version, group membership/include relationships including the conditional Tomli entry, docs-group Python bound, and pytest strict options. Assert CI triggers for pushes and pull requests targeting `main` plus manual dispatch, while benchmark smoke is dispatch-only; global `contents: read`, concurrency cancellation, job timeouts, setup-uv caching keyed by `uv.lock`, and the exact Python matrices (imports: 3.10/3.11/3.12; full tests: 3.10/3.12; quality, mypy, docs: 3.11). Walk every step in both workflows and require every `uses:` value to be a 40-character SHA from the audited checkout/setup-uv/upload-artifact set. For every checkout step, require `with.persist-credentials` to parse as the string `false`; for every setup-uv step, identify its dependency profile semantically from the job's commands/matrix and require the exact stable suffix mapping (`quality`, `docs`, `import-smoke`, `full-tests`, or `benchmark-smoke`), along with enabled caching and `uv.lock` as its dependency glob. Require install commands to use `uv sync --locked`, execution commands to use `uv run --no-sync`, and the docs command to contain `-n -W --keep-going`. Reject `pull_request_target`, write permissions, secret references, scheduled triggers, deployment, and publishing. Keep assertions semantic enough to permit harmless display-name/comment edits, but exact for security, versions, matrices, cache profiles, and commands.
  </action>
  <verify>
    <automated>test -x "$UV_BIN" &amp;&amp; "$UV_BIN" --version | grep -F "uv 0.12.8" &amp;&amp; "$UV_BIN" lock --check &amp;&amp; "$UV_BIN" sync --locked --only-group quality --python 3.11 &amp;&amp; "$UV_BIN" run --no-sync ruff check --output-format=github survarena tests scripts &amp;&amp; "$UV_BIN" run --no-sync python -m mypy survarena/core survarena/benchmark/resume.py survarena/data/splitters.py scripts/audit_manuscript_publishability.py &amp;&amp; export UV_BIN &amp;&amp; bash -euo pipefail -c 'for version in 3.10 3.11 3.12; do env_path=".venv-import-${version}"; UV_PROJECT_ENVIRONMENT="$env_path" "$UV_BIN" sync --locked --only-group import-smoke --python "$version"; UV_PROJECT_ENVIRONMENT="$env_path" "$UV_BIN" run --no-sync python -c "import survarena"; done' &amp;&amp; bash -euo pipefail -c 'for version in 3.10 3.12; do env_path=".venv-test-${version}"; UV_PROJECT_ENVIRONMENT="$env_path" "$UV_BIN" sync --locked --no-default-groups --group test --python "$version"; UV_PROJECT_ENVIRONMENT="$env_path" "$UV_BIN" run --no-sync python -m pytest tests/test_developer_infrastructure.py -q; UV_PROJECT_ENVIRONMENT="$env_path" "$UV_BIN" run --no-sync python -m pytest -q; UV_PROJECT_ENVIRONMENT="$env_path" "$UV_BIN" run --no-sync python -m compileall -q survarena; done' &amp;&amp; UV_PROJECT_ENVIRONMENT=.venv-docs "$UV_BIN" sync --locked --only-group docs --python 3.11 &amp;&amp; UV_PROJECT_ENVIRONMENT=.venv-docs "$UV_BIN" run --no-sync sphinx-build -n -W --keep-going -b html docs docs/_build/html</automated>
  </verify>
  <done>Pull requests and main pushes run immutable-action, credential-safe-checkout, profile-isolated cached, locked checks for Ruff, mypy, import compatibility, pytest/compile, and Sphinx; the manual semantic benchmark smoke uses the same lock with its own cache suffix; a focused regression test locks the workflow/dependency contract; job/Python strategy and local equivalence are documented without expanding permissions or release claims.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| PyPI metadata/artifacts -> local and CI environments | Third-party package names, versions, wheels, and source distributions enter the execution environment. |
| GitHub Marketplace actions -> hosted runner | Action code executes before project checks and can access the job token/cache. |
| Pull-request code/docs -> GitHub Actions | Untrusted fork content is parsed, imported in the deliberate lazy-import smoke, tested, and rendered. |
| uv cache -> job environment | Cached package artifacts are reused across workflow runs keyed by dependency state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-Q260901-01 | Tampering | `uv.lock` and dependency metadata | mitigate | Commit the generated lock, require uv 0.12.8, run `uv lock --check`, and use only `uv sync --locked` in CI. |
| T-Q260901-02 | Spoofing/Tampering | Workflow actions and checkout credentials | mitigate | Use only audited official actions pinned to immutable 40-character commit SHAs with version comments, and set `persist-credentials: false` on every checkout. |
| T-Q260901-03 | Elevation of privilege | Pull-request workflow token | mitigate | Keep `pull_request` rather than `pull_request_target`, set only `contents: read`, and provide no deployment permissions or secrets. |
| T-Q260901-04 | Tampering | uv package cache | mitigate | Let `setup-uv` manage the cache with `uv.lock` in the dependency key plus stable dependency-profile suffixes; validate the lock before use, avoid cross-profile cache reuse, and never cache `.venv`. |
| T-Q260901-05 | Denial of service | Heavy scientific dependency/test jobs | mitigate | Cancel superseded runs, retain explicit timeouts, install light groups for quality/docs/import jobs, and reserve the runtime stack for the two-version test matrix and manual smoke. |
| T-Q260901-06 | Information disclosure | Sphinx HTML artifact | accept | CI builds static narrative docs but does not upload or deploy them; repository content is already visible to the PR job. |
| T-Q260901-SC | Tampering | Python packages and GitHub Actions | mitigate | Package Legitimacy Audit above marks every introduced package/action VERIFIED; the lock and immutable action SHAs bind execution inputs. |
</threat_model>

<verification>
1. Follow `<dirty_worktree_protocol>` from the baseline manifest through the final byte-for-byte comparison; the implementation commit range may contain only `files_modified` and GSD quick-task artifacts.
2. From the detached post-task validation worktree, run the Task 3 automated command without overlays or Sphinx exclusions. A failure in lock freshness, lint, scoped typing, any declared Python lane, infrastructure assertions, full tests, compile, or strict docs blocks completion.
3. Confirm `tests/test_developer_infrastructure.py` passes under both full-test Python versions and semantically proves workflow triggers, permissions, concurrency, timeouts, matrices, credential-disabled checkout, exact dependency-profile cache suffixes, cache/lock flags, immutable action pins, and forbidden-feature absence.
4. Confirm the original dirty checkout's pre-existing files still match the baseline manifest after the detached validation worktree is removed.
5. Confirm `uv.lock`, documentation, and CI all encode Sphinx on Python 3.11 while package metadata explicitly supports Python 3.10-3.12.
</verification>

<source_coverage>

## Source Coverage Audit

| Source | ID | Feature/constraint | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | uv environment management with a committed lock and clear local commands | 1, 2 | COVERED | uv is primary; pip remains labeled compatibility. |
| GOAL | — | Sphinx documentation for the existing Markdown corpus | 1, 2 | COVERED | MyST avoids content migration; strict HTML is executable locally. |
| GOAL | — | GitHub Actions PR checks for Ruff and pytest | 3 | COVERED | Retains mypy/import/compile checks and adds docs/lock gates. |
| GOAL | — | Select essential, useful infrastructure without unnecessary complexity | 1-3 | COVERED | Explicit guardrails exclude deployment, publishing, formatting migration, coverage, and unrelated automation. |
| REQ | N/A | Quick task has no ROADMAP phase requirement IDs | — | N/A | Quick-full checker skips ROADMAP alignment. |
| RESEARCH | — | uv project lock, PEP 735 groups, `--locked`, and setup-uv caching | 1, 3 | COVERED | Based on official Astral documentation. |
| RESEARCH | — | Sphinx/MyST with nitpicky fail-on-warning build | 1, 2, 3 | COVERED | Sphinx 8.2 is selected to retain preferred Python 3.11 docs tooling. |
| RESEARCH | — | Explicit CI triggers, least privilege, concurrency, immutable action pins, and supported-Python strategy | 3 | COVERED | Full tests cover 3.10/3.12; 3.11 runs quality/docs/smoke. |
| RESEARCH | — | Ruff annotations without automatic fixes or premature formatting enforcement | 3 | COVERED | Existing lint scope is preserved. |
| RESEARCH | — | pytest 8 strict configuration | 1, 3 | COVERED | Strict config/markers and xfails are enabled without blanket warning policy. |
| CONTEXT | N/A | No quick-task CONTEXT.md or locked D-XX decisions supplied | — | N/A | Task boundary comes directly from the description and constraints. |
</source_coverage>

<success_criteria>
- `uv lock --check` succeeds and the committed lock is the only resolved developer/CI dependency artifact.
- Package metadata declares `>=3.10,<3.13`, `.python-version` selects 3.11, and a fresh Python 3.11 checkout can `uv sync --locked` and run the documented local quality commands through `uv run --no-sync`.
- `sphinx-build -n -W --keep-going -b html docs docs/_build/html` succeeds with every tracked guide in the toctree.
- The Mermaid fence in `docs/benchmarking_workflow.md` resolves through `TextLexer`, allowing the strict build to pass without a new renderer or source rewrite.
- CI retains main push, main pull request, and manual triggers; all jobs are read-only, cancellable, timeout-bounded, cached by `uv.lock` with the required stable profile suffix, action-pinned by SHA, and use checkout with persisted credentials disabled.
- `tests/test_developer_infrastructure.py` proves the dependency and workflow contract, while real commands prove Ruff, scoped mypy, lazy imports on 3.10/3.11/3.12, pytest/compile on 3.10/3.12, and Sphinx on 3.11 with locked dependencies.
- All final gates pass from a detached clean validation worktree, and every pre-existing dirty path in the original checkout remains byte-identical to its baseline.
- The manual WHAS500/CoxPH smoke remains manual-only and runs from the same lock.
- No application source, existing test changes, manuscript evidence, container scaffolding, formatting, deployment, or publication behavior changes.
</success_criteria>

<output>
Create `.planning/quick/260901-0gy-add-uv-environment-management-sphinx-doc/260901-0gy-SUMMARY.md` when done.
</output>
