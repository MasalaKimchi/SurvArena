---
quick_id: 260901-0fd
phase: quick-260901-0fd
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - docs/benchmark_readiness.md
  - docs/index.md

must_haves:
  truths:
    - "A reader can distinguish SurvArena's current status as an internal benchmark framework from the unmet gates for a research/citable benchmark and the deliberately deferred public/maintained benchmark tier."
    - "Every audit finding is explicitly classified as an observed defect, missing evidence, or future scope, so absence of proof is never presented as a confirmed bug and deliberate scope boundaries are not presented as failures."
    - "The register states where current benchmark outputs can be scientifically wrong, with exact live-repository evidence and authoritative external criteria adjacent to each claim."
    - "Every improvement row has a stable ID, accountable owner, severity, impact, confidence, remediation, verification or exit criterion, dependencies, status, and target maturity gate so it can be maintained across later phases."
    - "The readiness register covers the full audit rubric, remains distinct from manuscript evidence-status documents, and is discoverable from the documentation index."
    - "Every repository citation uses one canonical repository-relative path and exact line range, dirty cited files have content hashes, blocked external checks are explicitly routed to manual review, and validation never discloses credential-like values."
  artifacts:
    - path: "docs/benchmark_readiness.md"
      provides: "Source-backed benchmark maturity verdict, tier gates, defect ledger, and prioritized improvement register"
      contains: "Benchmark Readiness Register"
    - path: "docs/index.md"
      provides: "Single discoverability link to the readiness register without duplicating its content"
      contains: "benchmark_readiness.md"
  key_links:
    - from: "docs/benchmark_readiness.md"
      to: "current repository implementation and status files"
      via: "one-path-per-tag [VERIFIED: `path:start-end`] evidence citations plus dirty-file SHA-256 snapshot"
      pattern: "\\[VERIFIED:"
    - from: "docs/benchmark_readiness.md"
      to: "authoritative benchmark, survival-analysis, provenance, reporting, and governance criteria"
      via: "descriptive Markdown links adjacent to the criterion they support"
      pattern: "https://"
    - from: "docs/benchmark_readiness.md"
      to: "docs/manuscript_publishability.md and docs/test_status.md"
      via: "role-boundary cross-references for volatile evidence status"
      pattern: "(manuscript_publishability|test_status)\\.md"
    - from: "docs/index.md"
      to: "docs/benchmark_readiness.md"
      via: "one Reference Docs routing-table entry"
      pattern: "benchmark_readiness\\.md"
---

<objective>
Create SurvArena's durable benchmark-readiness register: a candid, source-backed assessment of what kind of benchmark it is today, where its conclusions can be wrong, which evidence is absent, what belongs to later public-platform scope, and what must be verified before stronger claims are allowed.

Purpose: Give future work one maintainable audit ledger instead of scattering benchmark-maturity judgments across transient research notes and manuscript-status snapshots.

Output: `docs/benchmark_readiness.md` plus one discoverability link in `docs/index.md`; no implementation, benchmark-result, configuration, or status-document changes.
</objective>

<execution_context>
@/Users/justin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/justin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@AGENTS.md
@.planning/quick/260901-0fd-audit-survarena-s-benchmark-readiness-ag/260901-0fd-RESEARCH.md
@.planning/quick/260901-0fd-audit-survarena-s-benchmark-readiness-ag/260901-0fd-VALIDATION.md
@README.md
@docs/index.md
@docs/protocol.md
@docs/manuscript_publishability.md
@docs/test_status.md
@docs/ci_and_reproducibility.md

<current_contracts>
- SurvArena is intentionally scoped to single-event, right-censored tabular survival and Python model ecosystems. Competing risks, recurrent events, multi-state outcomes, interval censoring, left truncation, time-varying prediction, non-Python methods, and a public submission service are scope boundaries, not current defects.
- The project is executing the scientific-comparison work now. Treat the research audit as a starting inventory, then verify every live-code citation against the worktree at execution time; do not preserve stale line numbers merely because they appear in the research artifact.
- `docs/manuscript_publishability.md` owns the current evidence-bundle verdict, `docs/test_status.md` owns observed test execution, `docs/ci_and_reproducibility.md` owns configured-versus-observed automation, and `.planning/REQUIREMENTS.md` owns milestone requirement state. The new register links to those files and explains their roles rather than copying their volatile tables.
- “Official” is not a universal certification. The register must define it as a project-specific three-tier maturity taxonomy and use named gate evidence, never an unqualified endorsement.
- Existing retained result matrices remain invalid as citable evidence after behavior-changing protocol work until they are regenerated under the frozen release recipe.
- The working tree contains unrelated application, test, manuscript, environment, and container work. Before either target document is edited, snapshot the complete staged/unstaged/untracked state and content identity of every already-dirty path. Modify and stage only `docs/benchmark_readiness.md`, `docs/index.md`, and the explicitly authorized quick-task artifacts; do not rewrite or clean unrelated changes. A final global comparison must allow unchanged pre-existing user changes while rejecting any new or changed path outside that allowlist.
</current_contracts>

<scope_guardrails>
- Do not implement fixes, edit source/config/test/result files, rerun expensive benchmark matrices, or change milestone status. This plan records evidence and exit criteria only.
- Do not turn the research artifact into a second long-form essay. Convert it into a compact maturity rubric, stable finding IDs, gate definitions, and a sortable improvement register with explicit maintenance rules.
- Do not claim that configured CI, an existing artifact, a pending requirement, or a locally passing smoke proves release reproducibility. Record observed evidence separately from planned or configured controls.
- Do not call extended survival tasks missing product functionality while the package remains accurately labeled. Record them as future scope that becomes a gap only if the project broadens its claim.
- Do not duplicate full manuscript coverage tables, test-command histories, roadmap prose, or implementation recipes already owned by linked artifacts.
- Do not include secrets, tokens, private paths outside the repository evidence already present in public-facing docs, or copied environment dumps.
</scope_guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the benchmark maturity verdict and evidence ledger</name>
  <files>docs/benchmark_readiness.md</files>
  <action>
When documenting provenance syntax, use the phrase “VERIFIED provenance tag” without emitting a placeholder literal beginning `[VERIFIED:`; the only literal occurrences permitted in the finished document are canonical evidence tags with real paths and numeric ranges.

Invoke the temporary guard's baseline mode exactly as `.venv/bin/python /tmp/survarena-260901-0fd-worktree-guard.py snapshot --output /tmp/survarena-260901-0fd-worktree-baseline.json` before any repository write; its `verify --baseline ... --allow ...` interface is exercised by both task-level checks below.

Before either target document is written, create `/tmp/survarena-260901-0fd-worktree-guard.py` and use it to capture `/tmp/survarena-260901-0fd-worktree-baseline.json`. The guard must read the repository globally with `git status --porcelain=v1 -z --untracked-files=all`; preserve both paths for renames; and record, for every staged, unstaged, or untracked path, its two-character Git status, worktree kind/content SHA-256 (or a missing sentinel), and index entry from `git ls-files --stage`. Its `verify` mode must recompute the same map and fail when any path outside the exact allowlist changes state, content, index identity, appears, or disappears. The allowlist is `docs/benchmark_readiness.md`, `docs/index.md`, and `.planning/quick/260901-0fd-audit-survarena-s-benchmark-readiness-ag/260901-0fd-SUMMARY.md`. Report only unauthorized path names and before/after state labels, never file contents. This snapshot is specifically required to preserve unchanged pre-existing user changes rather than treating the already-dirty worktree as this task's output.

Create `docs/benchmark_readiness.md` with the title “SurvArena Benchmark Readiness Register,” an audit date, evidence snapshot (current commit plus dirty-worktree caveat), scope, re-audit triggers, and a verdict led by the outcome: SurvArena is a useful internal benchmark framework/pre-release kernel, is not yet a research/citable benchmark release, and has deliberately not attempted the public/maintained tier. Define three exact tiers—Internal benchmark framework, Research/citable benchmark, and Public/maintained benchmark—with gate criteria, current status, evidence required, and the strongest naming each tier unlocks. State that “official” is meaningful only relative to a named version, ruleset, owner, and release process.

Define the evidence taxonomy before findings: `OBSERVED DEFECT` requires direct code behavior or an executable counterexample; `MISSING EVIDENCE` means the claim may be true but has not been demonstrated under the release contract; `FUTURE SCOPE` is deliberate work outside the present product claim; retain `[VERIFIED: \`path:start-end\`]`, `[CITED]`, and `[ASSUMED]` provenance semantics. Every `[VERIFIED:]` occurrence must be exactly one tag containing exactly one canonical repository-relative POSIX path and an explicit inclusive start/end range; do not combine paths or use absolute paths, `.`/`..` components, backslashes, symlink escapes, or omitted end lines. Under the exact heading `## Where SurvArena Is Wrong Today`, create one Markdown table—without literal pipe characters inside cells—with the exact columns `ID`, `Finding`, `Evidence class`, `Scientific / user consequence`, `Repository evidence`, `Authoritative criterion`, and `Confidence`. Its ID set is exactly `WRONG-01` through `WRONG-09`, once each, covering the nine research findings: Cartesian rather than matched pairwise wins, fold-inflated dataset ranks, fold/seed pseudo-replication in `n_pairs`, HPO-labeled rows that may not be tuned, non-killable timeout reporting, fold-varying `25/50/75` horizons, incomplete group safety, a non-canonical comprehensive artifact, and “manuscript-grade” intent without current release evidence. Every required cell is nonempty; `WRONG-01` through `WRONG-08` are `OBSERVED DEFECT`; `WRONG-09` is `MISSING EVIDENCE` because the maintained status proves the citable evidence is absent rather than proving a runtime defect; every repository-evidence cell cites inspected live lines; and every row's adjacent authoritative-criterion cell contains either a descriptive primary/official-source Markdown link or a cross-reference belonging to the exact `DOM-01` through `DOM-13` set.

Under `## Audit-Domain Coverage`, add one Markdown table with the exact columns `ID`, `Domain`, `Current assessment`, `Evidence class`, `Repository evidence`, `Authoritative criterion`, `Linked backlog IDs / rationale`, and `Assessment-change criterion`. Its ID set is exactly `DOM-01` through `DOM-13`, once each: task and claim contract; neutrality and baselines; dataset governance; leakage and resampling; HPO and compute fairness; survival metric validity; statistical comparison and multiplicity; robustness and generalizability; failures, provenance, and artifacts; CI and release reproducibility; manuscript reporting; public benchmark governance; and scope/security for extended tasks or untrusted submissions. Every required cell is nonempty; evidence classes use only the three defined values; authoritative criteria are descriptive primary/official-source links; and the linkage cell contains valid `BR-*` IDs or an explicit `No current gap — ...` rationale. Cross-reference the existing protocol, test-status, CI/reproducibility, manuscript-publishability, requirements, and roadmap artifacts in a short `## Document Ownership` section instead of repeating their changeable contents. Initialize a `## Dirty Cited-File Snapshot` table with the exact columns `Path`, `Git status at audit`, and `SHA-256`; Task 2 will refresh it after all citations are final.
  </action>
  <verify>
    <automated>.venv/bin/python -c '
from pathlib import Path
import re

s = Path("docs/benchmark_readiness.md").read_text(encoding="utf-8")

def table_after(heading):
    lines = s.splitlines()
    assert lines.count(heading) == 1, f"expected one heading: {heading}"
    start = lines.index(heading) + 1
    while start != len(lines) and not lines[start].startswith("|"):
        start += 1
    assert start + 1 &lt; len(lines), f"missing table after {heading}"
    raw = []
    while start != len(lines) and lines[start].startswith("|"):
        raw.append(lines[start])
        start += 1
    cells = lambda line: [cell.strip() for cell in line.strip()[1:-1].split("|")]
    assert len(raw) &gt;= 3 and all(set(cell) &lt;= {"-", ":"} for cell in cells(raw[1])), f"bad table delimiter: {heading}"
    header = cells(raw[0])
    rows = [dict(zip(header, cells(line), strict=True)) for line in raw[2:]]
    assert all(len(cells(line)) == len(header) for line in raw[2:]), f"ragged table: {heading}"
    return header, rows

required = ["SurvArena Benchmark Readiness Register", "Internal benchmark framework", "Research/citable benchmark", "Public/maintained benchmark", "OBSERVED DEFECT", "MISSING EVIDENCE", "FUTURE SCOPE", "## Document Ownership", "## Dirty Cited-File Snapshot"]
missing = [token for token in required if token not in s]
assert not missing, f"missing readiness sections/tokens: {missing}"

wrong_header, wrong_rows = table_after("## Where SurvArena Is Wrong Today")
assert wrong_header == ["ID", "Finding", "Evidence class", "Scientific / user consequence", "Repository evidence", "Authoritative criterion", "Confidence"], wrong_header
assert [row["ID"] for row in wrong_rows] == [f"WRONG-{i:02d}" for i in range(1, 10)], "WRONG rows must be exact, ordered, and unique"
assert all(all(row[column] for column in wrong_header) for row in wrong_rows), "empty WRONG cell"
expected_wrong_classes = {**{f"WRONG-{i:02d}": "OBSERVED DEFECT" for i in range(1, 9)}, "WRONG-09": "MISSING EVIDENCE"}
assert {row["ID"]: row["Evidence class"] for row in wrong_rows} == expected_wrong_classes, "WRONG evidence-class mapping"
assert all("[VERIFIED:" in row["Repository evidence"] for row in wrong_rows), "WRONG row lacks repository evidence"
valid_dom_ids = {f"DOM-{i:02d}" for i in range(1, 14)}
for row in wrong_rows:
    criterion = row["Authoritative criterion"]
    links = re.findall(r"\[([^]]+)\]\((https://[^)]+)\)", criterion)
    dom_occurrences = [match.start() for match in re.finditer(r"DOM-", criterion)]
    dom_matches = list(re.finditer(r"(?&lt;![A-Za-z0-9_-])DOM-(?:0[1-9]|1[0-3])(?![A-Za-z0-9_-])", criterion))
    dom_refs = [match.group(0) for match in dom_matches]
    assert all(label.strip() and not label.strip().startswith(("http://", "https://")) for label, _ in links), "criterion link needs a descriptive label: {}".format(row["ID"])
    assert criterion.count("https://") == len(links), "raw or malformed criterion URL: {}".format(row["ID"])
    assert dom_occurrences == [match.start() for match in dom_matches] and all(ref in valid_dom_ids for ref in dom_refs), "unknown or malformed criterion cross-reference: {}".format(row["ID"])
    assert links or dom_refs, "WRONG row lacks adjacent criterion: {}".format(row["ID"])

dom_header, dom_rows = table_after("## Audit-Domain Coverage")
assert dom_header == ["ID", "Domain", "Current assessment", "Evidence class", "Repository evidence", "Authoritative criterion", "Linked backlog IDs / rationale", "Assessment-change criterion"], dom_header
assert [row["ID"] for row in dom_rows] == [f"DOM-{i:02d}" for i in range(1, 14)], "DOM rows must be exact, ordered, and unique"
assert all(all(row[column] for column in dom_header) for row in dom_rows), "empty DOM cell"
assert all(row["Evidence class"] in {"OBSERVED DEFECT", "MISSING EVIDENCE", "FUTURE SCOPE"} for row in dom_rows), "invalid DOM evidence class"
assert all(re.search(r"\[[^]]+\]\(https://[^)]+\)", row["Authoritative criterion"]) for row in dom_rows), "DOM row lacks authoritative link"
assert all(re.search(r"BR-[0-9]{3}|No current gap — .+", row["Linked backlog IDs / rationale"]) for row in dom_rows), "DOM row lacks backlog mapping or rationale"
' &amp;&amp; .venv/bin/python /tmp/survarena-260901-0fd-worktree-guard.py verify --baseline /tmp/survarena-260901-0fd-worktree-baseline.json --allow docs/benchmark_readiness.md --allow docs/index.md --allow .planning/quick/260901-0fd-audit-survarena-s-benchmark-readiness-ag/260901-0fd-SUMMARY.md</automated>
    <human-check>Read `WRONG-01` through `WRONG-08` beside their cited live lines and block or narrow any wording not directly supported; confirm `WRONG-09` remains `MISSING EVIDENCE` and is framed as an unsupported maturity claim rather than a runtime defect. For every row, confirm its descriptive source link or exact `DOM-01..13` cross-reference states the adjacent authoritative criterion.</human-check>
  </verify>
  <done>The document gives an unambiguous current verdict, defines all three “official” maturity tiers and gates, explains the evidence classes, records the exact nine source-backed wrong-today rows with adjacent criteria, covers the exact 13 audit domains with backlog mappings, delegates volatile status details to the existing owner documents, and leaves all non-allowlisted worktree state byte-for-byte/index-for-index unchanged from the pre-task snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Build the prioritized register and close documentation evidence gates</name>
  <files>docs/benchmark_readiness.md, docs/index.md</files>
  <action>
Use the exact heading `## Prioritized Improvement Register` for the register table described below.

Complete the readiness document with one prioritized-register Markdown table whose IDs are unique canonical `BR-NNN` values and whose exact columns are `ID`, `Finding`, `Evidence class`, `Severity`, `Impact`, `Confidence`, `Owner`, `Status`, `Repository evidence`, `Authoritative criterion`, `Remediation`, `Verification / exit criterion`, `Dependencies`, `Target gate`, `Related phase / requirement`, and `Last verified`. Name `Benchmark maintainer` as the register steward and define the only acceptable owner roles as `Benchmark maintainer`, `Scientific-methods maintainer`, `Data steward`, `Release steward`, and `Security maintainer`; assign one to every row. Use only evidence classes `OBSERVED DEFECT`, `MISSING EVIDENCE`, or `FUTURE SCOPE`; severities `P0`, `P1`, or `P2`; confidence values `HIGH`, `MEDIUM`, or `LOW`; statuses `open`, `in_progress`, `blocked`, `verified`, `accepted_risk`, `deferred_scope`, or `superseded`; and targets `Gate A`, `Gate B`, `Gate C`, or `Broadened claim`. Cover every research finding without collapsing distinct acceptance tests: comparison matching/inference; common support and reliability; report identity; group-safe validation; frozen horizons/IPCW and prediction validity; truthful HPO states and budgets; killable resource isolation; immutable dataset releases; conflict-rejecting canonical provenance; typed protocol/seed semantics; null and out-of-fold ensemble baselines; calibration/clinical-utility scope; robustness labeling and breadth; locked CI/independent reproduction; manuscript evidence regeneration; external adapter/submission conformance; public governance/anti-overfitting; extended survival tasks; and public-service security boundaries. Keep P0/P1 gaps tied to the scientific and citable gates, while P2 rows are plainly marked as public-platform or broadened-claim scope rather than blockers for the current package boundary.

Add `## Research Coverage Crosswalk` with exact columns `Research item`, `Register IDs`, and `Coverage note`. Include exactly and once each `P0-A` through `P0-I`, `P1-A` through `P1-F`, `P2-A` through `P2-C`, `VALIDATION`, `SECURITY`, `DECISIONS`, and `ASSUMPTIONS`; every item must map to at least one existing `BR-NNN`, and every register row must be reachable from this crosswalk. Resolve Task 1's `DOM-01` through `DOM-13` linkage cells against the final register: every cited `BR-NNN` must exist, and a domain without a row must retain a substantive `No current gap — ...` rationale. Add a concise decisions-needed section for claim purpose, tuning target, default-only methods in tuned comparisons, common-support policy, and canonical hardware/determinism. Add maintenance rules defining the allowed statuses, owner-role responsibilities, closure evidence, line-citation refresh, and re-audit triggers after comparison-kernel, protocol/execution, suite/roster, reporting, or release-environment changes. A row may become `verified` only when its own exit criterion has observed evidence; a configured workflow or pending requirement is insufficient. Preserve historical IDs when rows close or split, and record replacements rather than deleting audit history.

Validate all repository provenance with a whole-document scan, not a best-effort regex subset. Count every literal `[VERIFIED:` start and require its span to match exactly `[VERIFIED: \`path:start-end\`]`; reject malformed tags, multiple paths in one tag, absolute paths, backslashes, `.`/`..` path components, noncanonical POSIX spelling, symlinks resolving outside the repository, untracked evidence targets, missing files, and out-of-range or reversed lines. After citations are final, refresh `## Dirty Cited-File Snapshot`: it must contain exactly the cited files reported dirty by global `git status --porcelain=v1 --untracked-files=all`, with the exact two-character Git status in code formatting and the current lowercase SHA-256 of each file's bytes; clean cited files must not appear. Then inspect each cited range for semantic accuracy.

Validate local Markdown links and heading fragments in both changed docs while enforcing repository containment. Validate every authoritative external link with a redirect-following GET that records HTTP result and effective destination. Classify `403`, `406`, `418`, `429`, `451`, curl/network failures, and `5xx` publisher failures as `manual_required` rather than success; classify other non-`2xx`/`3xx` results as invalid and replace them. Under `## Manual External-Source Checks`, create a table with exact columns `URL`, `Automation result`, `Final destination`, `Source identity`, and `Checked on`; it may be empty when no link is blocked, but every `manual_required` URL must have one row recording the response, the manually opened final destination, `confirmed — ...` authoritative-source identity, and an ISO date. Manually open every external criterion link—including automated successes—to confirm its final destination and primary/official source identity; HTTP reachability alone is not semantic validation.

Add exactly one Markdown-link row in the existing `docs/index.md` `## Reference Docs` table describing `benchmark_readiness.md` as the maturity verdict and improvement register; do not add a README link or a second plain-text/link occurrence. Scan both changed docs for AWS, GitHub, OpenAI, Hugging Face `hf_...`, private-key, bearer-token, and generic credential-assignment patterns. The scanner must retain and report only `file`, `line`, and named `pattern`, with the matched value redacted and never interpolated into an assertion or log. Check trailing whitespace and terminal newlines directly from both files so an untracked new readiness document is covered, then run the global `/tmp/survarena-260901-0fd-worktree-guard.py verify` comparison against the pre-task snapshot; a new, changed, staged, unstaged, removed, or untracked path outside the exact allowlist blocks completion even when unrelated paths were already dirty.
  </action>
  <verify>
    <automated>.venv/bin/python -c '
from pathlib import Path
import re

readiness = Path("docs/benchmark_readiness.md").read_text(encoding="utf-8")
index = Path("docs/index.md").read_text(encoding="utf-8")

def table_after(text, heading, allow_empty=False):
    lines = text.splitlines()
    assert lines.count(heading) == 1, f"expected one heading: {heading}"
    start = lines.index(heading) + 1
    while start != len(lines) and not lines[start].startswith("|"):
        start += 1
    assert start + 1 &lt; len(lines), f"missing table after {heading}"
    raw = []
    while start != len(lines) and lines[start].startswith("|"):
        raw.append(lines[start])
        start += 1
    cells = lambda line: [cell.strip() for cell in line.strip()[1:-1].split("|")]
    assert len(raw) &gt;= 2 and all(set(cell) &lt;= {"-", ":"} for cell in cells(raw[1])), f"bad table delimiter: {heading}"
    header = cells(raw[0])
    body = raw[2:]
    assert allow_empty or body, f"empty table: {heading}"
    assert all(len(cells(line)) == len(header) for line in body), f"ragged table: {heading}"
    return header, [dict(zip(header, cells(line), strict=True)) for line in body]

register_header, register_rows = table_after(readiness, "## Prioritized Improvement Register")
expected_register_header = ["ID", "Finding", "Evidence class", "Severity", "Impact", "Confidence", "Owner", "Status", "Repository evidence", "Authoritative criterion", "Remediation", "Verification / exit criterion", "Dependencies", "Target gate", "Related phase / requirement", "Last verified"]
assert register_header == expected_register_header, register_header
assert all(all(row[column] for column in register_header) for row in register_rows), "empty register cell"
register_ids = [row["ID"] for row in register_rows]
assert len(register_ids) == len(set(register_ids)) and all(re.fullmatch(r"BR-[0-9]{3}", item) for item in register_ids), "invalid or duplicate BR IDs"
assert all(row["Evidence class"] in {"OBSERVED DEFECT", "MISSING EVIDENCE", "FUTURE SCOPE"} for row in register_rows), "invalid evidence class"
assert all(row["Severity"] in {"P0", "P1", "P2"} for row in register_rows), "invalid severity"
assert all(row["Confidence"] in {"HIGH", "MEDIUM", "LOW"} for row in register_rows), "invalid confidence"
owners = {"Benchmark maintainer", "Scientific-methods maintainer", "Data steward", "Release steward", "Security maintainer"}
assert all(row["Owner"] in owners for row in register_rows), "invalid owner role"
statuses = {"open", "in_progress", "blocked", "verified", "accepted_risk", "deferred_scope", "superseded"}
assert all(row["Status"] in statuses for row in register_rows), "invalid status"
assert all(row["Target gate"] in {"Gate A", "Gate B", "Gate C", "Broadened claim"} for row in register_rows), "invalid target gate"
assert "**Register steward:** Benchmark maintainer" in readiness, "register steward not named"
assert all(role in readiness for role in owners), "owner roles not defined"

cross_header, cross_rows = table_after(readiness, "## Research Coverage Crosswalk")
assert cross_header == ["Research item", "Register IDs", "Coverage note"], cross_header
expected_research = [*[f"P0-{letter}" for letter in "ABCDEFGHI"], *[f"P1-{letter}" for letter in "ABCDEF"], *[f"P2-{letter}" for letter in "ABC"], "VALIDATION", "SECURITY", "DECISIONS", "ASSUMPTIONS"]
assert [row["Research item"] for row in cross_rows] == expected_research, "research crosswalk set/order mismatch"
assert all(all(row[column] for column in cross_header) for row in cross_rows), "empty research crosswalk cell"
register_set = set(register_ids)
crosswalk_ids = []
for row in cross_rows:
    mapped = re.findall(r"BR-[0-9]{3}", row["Register IDs"])
    assert mapped and set(mapped) &lt;= register_set, "bad research mapping: {}".format(row["Research item"])
    crosswalk_ids.extend(mapped)
assert register_set &lt;= set(crosswalk_ids), "orphan register row not reachable from research crosswalk"

dom_header, dom_rows = table_after(readiness, "## Audit-Domain Coverage")
assert dom_header == ["ID", "Domain", "Current assessment", "Evidence class", "Repository evidence", "Authoritative criterion", "Linked backlog IDs / rationale", "Assessment-change criterion"], dom_header
assert [row["ID"] for row in dom_rows] == [f"DOM-{i:02d}" for i in range(1, 14)], "DOM set/order mismatch"
for row in dom_rows:
    mapped = re.findall(r"BR-[0-9]{3}", row["Linked backlog IDs / rationale"])
    assert (mapped and set(mapped) &lt;= register_set) or re.fullmatch(r"No current gap — .+", row["Linked backlog IDs / rationale"]), "unresolved DOM mapping: {}".format(row["ID"])

reference_header, reference_rows = table_after(index, "## Reference Docs")
assert reference_header == ["Topic", "Read"], reference_header
routing_rows = [row for row in reference_rows if re.search(r"\[[^]]+\]\(benchmark_readiness\.md\)", row["Read"])]
assert len(routing_rows) == 1, "Reference Docs must have exactly one readiness row"
assert len(re.findall(r"benchmark_readiness\.md", index)) == 1, "docs index must contain exactly one readiness occurrence"
' &amp;&amp; .venv/bin/python -c '
from pathlib import Path, PurePosixPath
from urllib.parse import unquote, urlsplit
import hashlib
import re
import subprocess

repo = Path(subprocess.run(["git", "rev-parse", "--show-toplevel"], check=True, capture_output=True, text=True).stdout.strip()).resolve()
readiness_path = repo / "docs/benchmark_readiness.md"
text = readiness_path.read_text(encoding="utf-8")

tag_re = re.compile(r"\[VERIFIED: `(?P&lt;path&gt;[A-Za-z0-9_.-]+(?:/[A-Za-z0-9_.-]+)*):(?P&lt;start&gt;[1-9][0-9]*)-(?P&lt;end&gt;[1-9][0-9]*)`\]")
occurrences = [match.start() for match in re.finditer(r"\[VERIFIED:", text)]
tags = list(tag_re.finditer(text))
assert occurrences and occurrences == [match.start() for match in tags], "every VERIFIED occurrence must be one exact canonical path:start-end tag"

cited = set()
for tag in tags:
    rel = tag.group("path")
    posix = PurePosixPath(rel)
    assert not posix.is_absolute() and rel == posix.as_posix() and "\\" not in rel and all(part not in {"", ".", ".."} for part in posix.parts), f"noncanonical citation path: {rel}"
    resolved = (repo / rel).resolve()
    assert resolved.is_relative_to(repo) and resolved.is_file(), f"citation escapes or is missing: {rel}"
    tracked = subprocess.run(["git", "ls-files", "--error-unmatch", "--", rel], capture_output=True, text=True)
    assert tracked.returncode == 0, f"citation target is not tracked: {rel}"
    line_count = len(resolved.read_text(encoding="utf-8").splitlines())
    start = int(tag.group("start"))
    end = int(tag.group("end"))
    assert start in range(1, line_count + 1) and end in range(1, line_count + 1) and start &lt;= end, f"citation range invalid: {rel}:{start}-{end}"
    cited.add(rel)

status_output = subprocess.run(["git", "status", "--porcelain=v1", "--untracked-files=all"], check=True, capture_output=True, text=True).stdout.splitlines()
dirty = {}
for line in status_output:
    status = line[:2]
    payload = line[3:]
    paths = payload.split(" -&gt; ") if " -&gt; " in payload else [payload]
    for rel in paths:
        dirty[rel] = status

def table_after(heading):
    lines = text.splitlines()
    assert lines.count(heading) == 1, f"expected one heading: {heading}"
    start = lines.index(heading) + 1
    while start != len(lines) and not lines[start].startswith("|"):
        start += 1
    raw = []
    while start != len(lines) and lines[start].startswith("|"):
        raw.append(lines[start])
        start += 1
    cells = lambda line: [cell.strip() for cell in line.strip()[1:-1].split("|")]
    assert len(raw) &gt;= 2
    header = cells(raw[0])
    assert all(len(cells(line)) == len(header) for line in raw[2:])
    return header, [dict(zip(header, cells(line), strict=True)) for line in raw[2:]]

hash_header, hash_rows = table_after("## Dirty Cited-File Snapshot")
assert hash_header == ["Path", "Git status at audit", "SHA-256"], hash_header
recorded = {}
for row in hash_rows:
    assert re.fullmatch(r"`[^`]+`", row["Path"]) and re.fullmatch(r"`.{2}`", row["Git status at audit"]), "snapshot path/status must be code-formatted"
    rel = row["Path"][1:-1]
    assert rel not in recorded, f"duplicate snapshot row: {rel}"
    recorded[rel] = (row["Git status at audit"][1:-1], row["SHA-256"])
expected_dirty = {rel: dirty[rel] for rel in cited if rel in dirty}
assert set(recorded) == set(expected_dirty), f"dirty cited-file set mismatch: recorded={sorted(recorded)}, expected={sorted(expected_dirty)}"
for rel, status in expected_dirty.items():
    assert recorded[rel][0] == status, f"dirty status mismatch: {rel}"
    digest = hashlib.sha256((repo / rel).read_bytes()).hexdigest()
    assert recorded[rel][1] == digest and re.fullmatch(r"[0-9a-f]{64}", digest), f"dirty citation hash mismatch: {rel}"

def heading_slugs(path):
    counts = {}
    slugs = set()
    for line in path.read_text(encoding="utf-8").splitlines():
        match = re.match(r"^#{1,6}\s+(.+?)\s*#*$", line)
        if not match:
            continue
        raw = re.sub(r"&lt;[^&gt;]+&gt;", "", match.group(1))
        raw = re.sub(r"[`*_~]", "", raw).lower()
        base = re.sub(r"[^\w\- ]", "", raw)
        base = re.sub(r"\s+", "-", base.strip())
        number = counts.get(base, 0)
        counts[base] = number + 1
        slugs.add(base if number == 0 else f"{base}-{number}")
    return slugs

for source_rel in ["docs/benchmark_readiness.md", "docs/index.md"]:
    source = repo / source_rel
    source_text = source.read_text(encoding="utf-8")
    for target in re.findall(r"(?&lt;!\!)\[[^]]+\]\(([^)]+)\)", source_text):
        parts = urlsplit(target)
        if parts.scheme in {"http", "https"}:
            continue
        assert not parts.scheme and not parts.netloc, f"unsupported local-link scheme in {source_rel}: {target}"
        target_path = source if not parts.path else (source.parent / unquote(parts.path)).resolve()
        assert target_path.resolve().is_relative_to(repo) and target_path.is_file(), f"missing or escaping local link in {source_rel}: {target}"
        if parts.fragment:
            assert unquote(parts.fragment) in heading_slugs(target_path), f"missing local anchor in {source_rel}: {target}"
' &amp;&amp; .venv/bin/python -c '
from pathlib import Path
import concurrent.futures as futures
import re
import subprocess

text = Path("docs/benchmark_readiness.md").read_text(encoding="utf-8")
urls = sorted(set(re.findall(r"(?&lt;!\!)\[[^]]+\]\((https://[^)]+)\)", text)))
assert urls, "no authoritative external links"

def check(url):
    run = subprocess.run(["curl", "-L", "-sS", "--connect-timeout", "4", "--max-time", "12", "-o", "/dev/null", "-w", "%{http_code}\t%{url_effective}", url], capture_output=True, text=True)
    fields = run.stdout.strip().split("\t", 1)
    code = int(fields[0]) if fields and fields[0].isdigit() else 0
    effective = fields[1] if len(fields) == 2 else url
    if run.returncode == 0 and code in range(200, 400):
        state = "ok"
    elif run.returncode != 0 or code in {403, 406, 418, 429, 451} or code in range(500, 600):
        state = "manual_required"
    else:
        state = "invalid"
    return url, state, code, effective, run.returncode

with futures.ThreadPoolExecutor(max_workers=8) as pool:
    results = list(pool.map(check, urls))
for url, state, code, effective, returncode in results:
    print(f"{state}\tHTTP={code}\tcurl={returncode}\t{url}\tfinal={effective}")
invalid = [(url, code, effective) for url, state, code, effective, _ in results if state == "invalid"]
assert not invalid, f"invalid external links: {invalid}"

lines = text.splitlines()
heading = "## Manual External-Source Checks"
assert lines.count(heading) == 1, "manual source-check section missing"
start = lines.index(heading) + 1
while start != len(lines) and not lines[start].startswith("|"):
    start += 1
raw = []
while start != len(lines) and lines[start].startswith("|"):
    raw.append(lines[start])
    start += 1
cells = lambda line: [cell.strip() for cell in line.strip()[1:-1].split("|")]
assert len(raw) &gt;= 2
header = cells(raw[0])
assert header == ["URL", "Automation result", "Final destination", "Source identity", "Checked on"], header
assert all(len(cells(line)) == len(header) for line in raw[2:])
rows = [dict(zip(header, cells(line), strict=True)) for line in raw[2:]]
records = {}
for row in rows:
    match = re.search(r"\[[^]]+\]\((https://[^)]+)\)", row["URL"])
    assert match and match.group(1) not in records, "manual source URL must be a unique Markdown link"
    records[match.group(1)] = row
for url, state, code, effective, returncode in results:
    if state != "manual_required":
        continue
    assert url in records, f"manual_required link lacks review row: {url}"
    row = records[url]
    assert row["Automation result"].startswith("manual_required"), f"bad manual result: {url}"
    assert re.search(r"https://", row["Final destination"]), f"missing final destination: {url}"
    assert row["Source identity"].startswith("confirmed — "), f"source identity not confirmed: {url}"
    assert re.fullmatch(r"[0-9]{4}-[0-9]{2}-[0-9]{2}", row["Checked on"]), f"manual check date missing: {url}"
' &amp;&amp; .venv/bin/python -c '
from pathlib import Path
import json
import re

files = [Path("docs/benchmark_readiness.md"), Path("docs/index.md")]
patterns = {
    "aws_access_key": re.compile(r"AKIA[0-9A-Z]{16}"),
    "github_token": re.compile(r"gh[pousr]_[A-Za-z0-9]{30,}"),
    "openai_token": re.compile(r"sk-[A-Za-z0-9_-]{20,}"),
    "hugging_face_token": re.compile(r"hf_[A-Za-z0-9]{20,}"),
    "private_key": re.compile(r"-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----"),
    "bearer_token": re.compile(r"(?i)\bbearer\s+[A-Za-z0-9._~+/=-]{16,}"),
    "credential_assignment": re.compile(r"(?i)\b(?:api[_-]?key|access[_-]?token|auth[_-]?token|secret|password)\s*[:=]\s*[\"\047]?[A-Za-z0-9._~+/=-]{8,}"),
}
hits = []
whitespace = []
for path in files:
    content = path.read_text(encoding="utf-8")
    assert content.endswith("\n"), f"missing terminal newline: {path}"
    for line_number, line in enumerate(content.splitlines(), start=1):
        if line.endswith((" ", "\t")):
            whitespace.append((str(path), line_number))
        for name, pattern in patterns.items():
            if pattern.search(line):
                hits.append({"file": str(path), "line": line_number, "pattern": name})
assert not whitespace, f"trailing whitespace: {whitespace}"
assert not hits, "credential-like matches (values redacted): " + json.dumps(hits, sort_keys=True)
' &amp;&amp; git diff --check -- docs/benchmark_readiness.md docs/index.md &amp;&amp; .venv/bin/python /tmp/survarena-260901-0fd-worktree-guard.py verify --baseline /tmp/survarena-260901-0fd-worktree-baseline.json --allow docs/benchmark_readiness.md --allow docs/index.md --allow .planning/quick/260901-0fd-audit-survarena-s-benchmark-readiness-ag/260901-0fd-SUMMARY.md</automated>
    <human-check>Inspect every `[VERIFIED:]` range for semantic support; open every external criterion link and confirm its final destination and primary/official source identity, recording each automation-blocked URL as `manual_required`; confirm every P0/P1 exit criterion demands falsifiable observed evidence; and review the global guard result to ensure all pre-existing out-of-scope changes are unchanged.</human-check>
  </verify>
  <done>Every researched gap or scope item is represented by an owned, maintainable, dependency-aware register row and the exact research crosswalk; all audit domains resolve to valid register IDs or a rationale; every canonical repository citation and dirty-file hash validates; all local anchors and authoritative sources are checked with blocked links explicitly recorded for manual confirmation; the docs index contains exactly one routing-table link; secret and whitespace checks cover both files without echoing values; and the global worktree comparison reports no change outside the exact allowlist.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutable worktree -> readiness claims | Active Phase 2 changes can make a previously correct path/line citation stale or change the behavior being described. |
| External publication/site -> benchmark criterion | A secondary, moved, or misread source can be presented as an authoritative requirement. |
| Audit register -> future implementation/release decisions | Ambiguous classifications or unverifiable closure criteria can cause maintainers to overstate maturity or prioritize the wrong work. |
| Local notes/environment -> tracked documentation | Copied commands or diagnostics can accidentally introduce credentials, private paths, or other sensitive literals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-Q260901-0FD-01 | Tampering | Repository evidence citations | mitigate | Validate every literal tag against one canonical repository-relative tracked path and explicit range, reject escapes/malformed occurrences, hash every cited file that is dirty at audit time, and semantically inspect the cited lines. |
| T-Q260901-0FD-02 | Spoofing | External benchmark criteria | mitigate | Use descriptive links to primary papers or official standards/projects, record effective destinations, label automation blocks `manual_required`, manually confirm source identity, and label the three-tier maturity model as a project synthesis rather than a certification. |
| T-Q260901-0FD-03 | Repudiation | Finding status and closure | mitigate | Assign stable IDs, an allowed owner role, a named register steward, evidence classes, last-verified dates, dependencies, and falsifiable exit criteria; preserve superseded history instead of deleting rows. |
| T-Q260901-0FD-04 | Information disclosure | `docs/benchmark_readiness.md`, `docs/index.md` | mitigate | Limit content to public repository evidence and authoritative links, then run a named-pattern scan that reports only file/line/pattern with values redacted, including Hugging Face and bearer-token forms. |
| T-Q260901-0FD-05 | Denial of service | External link validation | accept | Use bounded parallel checks; classify publisher/network blocks as `manual_required` and require recorded manual destination/source confirmation instead of silently treating blocked HTTP codes as success. |
| T-Q260901-0FD-06 | Elevation of privilege | Documentation-only task | accept | No executable code, dependency, workflow, permission, or service boundary is changed; future public submissions remain explicitly outside the trusted local CLI boundary. |
</threat_model>

<verification>
1. Confirm the pre-edit global worktree snapshot exists, then run the global post-task comparator. It must compare staged state, unstaged state, untracked paths, index identity, and worktree hashes for every path; unchanged pre-existing user changes pass, while any new or changed path outside `docs/benchmark_readiness.md`, `docs/index.md`, and the exact quick-task summary path fails.
2. Run both task-level automated commands. Any wrong table schema or ID set, empty required cell, invalid owner/status/classification, missing research crosswalk, unresolved domain mapping, malformed or escaping repository citation, dirty-citation hash mismatch, invalid local anchor, unresolved external-source disposition, credential-pattern hit, or whitespace error blocks completion.
3. For every `OBSERVED DEFECT` and every P0/P1 register row, inspect the cited live code/status lines—not merely file existence—and confirm the wording, impact, confidence, evidence class, and adjacent authoritative criterion are justified.
4. Confirm the exact `WRONG-01` through `WRONG-09` and `DOM-01` through `DOM-13` sets; resolve every domain to an existing `BR-NNN` or substantive no-current-gap rationale; and confirm the exact research crosswalk covers all P0, P1, P2, validation, security, open-decision, and assumptions items without orphan register rows.
5. Confirm the maturity gate table says what evidence unlocks each claim and never uses “official” without a named version, ruleset, owner, or release process.
6. Confirm `docs/manuscript_publishability.md`, `docs/test_status.md`, `docs/ci_and_reproducibility.md`, `.planning/REQUIREMENTS.md`, and `.planning/ROADMAP.md` remain the owners of their volatile details and are linked rather than duplicated.
7. Review every external criterion link's final destination and primary/official source identity; ensure every automation-blocked link is reported as `manual_required` with a completed manual record rather than accepted as an HTTP success.
8. Confirm `Benchmark maintainer` is the named register steward, every register row has one allowed owner role, and maintenance rules explain role responsibilities and reassignment.
9. Review the final diff for candid language: current mathematical/reporting defects are direct, missing proof is not called a bug, and extended/public-platform scope is not presented as a blocker for the accurately scoped local toolkit.
</verification>

<source_coverage>

## Source Coverage Audit

| Source | ID | Feature/constraint | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Assess SurvArena's benchmark legitimacy, deficiencies, failures, improvement opportunities, and wrong assumptions | 1, 2 | COVERED | Tier verdict, wrong-today ledger, domain matrix, and improvement register directly answer the request. |
| GOAL | — | Produce a durable document for future tracking | 2 | COVERED | Stable IDs, statuses, dependencies, exit criteria, dates, ownership, and re-audit rules make the artifact maintainable. |
| REQ | N/A | Quick-full task has no ROADMAP phase requirement IDs | — | N/A | No phase requirement mapping applies. |
| RESEARCH | — | Three-tier meaning of “official” and naming gates | 1 | COVERED | Internal framework, research/citable, and public/maintained tiers have explicit gates and current status. |
| RESEARCH | P0-A | Matched comparison, ranking scale, dataset-level inference, and multiplicity defects | 1, 2 | COVERED | Wrong-today findings plus separate P0 register rows and counterexample exit criteria. |
| RESEARCH | P0-B | Conditional-on-success headline ranking and absent common-support/reliability contract | 1, 2 | COVERED | Classified as observed behavior with scientific impact and a release gate. |
| RESEARCH | P0-C | Report identity rewriting/duplication and non-canonical report inputs | 1, 2 | COVERED | Separate identity/provenance finding and canonical-query exit criterion. |
| RESEARCH | P0-D | Group identifier leakage and non-group-safe inner/early-stop validation | 1, 2 | COVERED | Separate leakage finding with structural and adversarial-test exit criteria. |
| RESEARCH | P0-E | Mutable horizons/IPCW support and insufficient prediction validity contracts | 1, 2 | COVERED | Separate metric/support/capability finding tied to differential and invalid-output evidence. |
| RESEARCH | P0-F | Misleading HPO labels, unavailable tuner behavior, and conflated budgets | 1, 2 | COVERED | Wrong-today finding and truthful-state/budget register row. |
| RESEARCH | P0-G | Thread timeout without process containment | 1, 2 | COVERED | Wrong-today finding and hanging-process exit criterion. |
| RESEARCH | P0-H | Dataset releases lack immutable identity, citation, licensing, and endpoint metadata | 2 | COVERED | Dataset-governance row tied to the citable gate. |
| RESEARCH | P0-I | Canonical collection/provenance is not the live source of truth and conflicts can overwrite | 1, 2 | COVERED | Wrong-today finding plus conflict/idempotency/report-regeneration criteria. |
| RESEARCH | P1-A | Typed task contract and inconsistent seed/repeat semantics | 2 | COVERED | Dedicated P1 register row. |
| RESEARCH | P1-B | Missing null/sanity and leakage-safe portfolio baselines | 2 | COVERED | Dedicated P1 register row. |
| RESEARCH | P1-C | Calibration and clinical-utility metrics overstate their current generality | 2 | COVERED | Dedicated P1 scope/validation row. |
| RESEARCH | P1-D | Robustness tracks are narrow or mislabeled | 2 | COVERED | Dedicated P1 taxonomy/coverage row. |
| RESEARCH | P1-E | CI shows local software health, not locked release reproduction | 1, 2 | COVERED | Evidence class and independent-reproduction gate are explicit. |
| RESEARCH | P1-F | Manuscript evidence is invalidated/incomplete | 1, 2 | COVERED | Readiness finding links to the owner status doc; no evidence table is duplicated. |
| RESEARCH | P2-A | External adapter/submission extensibility | 2 | COVERED | Marked future public-platform scope with versioning/conformance criteria. |
| RESEARCH | P2-B | Public leaderboard governance, audit, appeals, errata, and anti-overfitting | 1, 2 | COVERED | Public/maintained tier and P2 row. |
| RESEARCH | P2-C | Extended survival tasks require separately versioned contracts | 1, 2 | COVERED | Explicit future-scope classification, not a current defect. |
| RESEARCH | RUBRIC | All external audit domains and authoritative primary/official sources | 1 | COVERED | `DOM-01` through `DOM-13` provide full-domain coverage and criterion links. |
| RESEARCH | VALIDATION | Constructed/differential/leakage/resource/package/container/reproduction exit evidence | 2 | COVERED | Rows require objective observed evidence rather than implementation completion. |
| RESEARCH | SECURITY | Local CLI trust boundary and future untrusted-submission controls | 1, 2 | COVERED | Security is a domain row and future public-service dependency, without recasting local auth as a current gap. |
| RESEARCH | DECISIONS | Claim purpose, tuning target, default-only comparison, common support, and hardware/determinism choices | 2 | COVERED | Dedicated decisions-needed section. |
| RESEARCH | ASSUMPTIONS | Taxonomy is synthesized, worktree is moving, and audit is not exhaustive numerical validation | 1, 2 | COVERED | Scope/metadata and evidence labels retain all three limitations. |
| CONTEXT | N/A | No quick-task CONTEXT.md or locked D-XX decisions supplied | — | N/A | Scope and constraints come from the task description and completed research. |
</source_coverage>

<success_criteria>
- `docs/benchmark_readiness.md` states the current benchmark-maturity verdict before implementation detail and defines gate criteria for internal framework, research/citable, and public/maintained tiers.
- The document contains exactly nine stable wrong-today findings, each with consequence, evidence class, confidence, canonical current repository evidence, and an adjacent authoritative criterion or criterion cross-reference.
- All 13 audit domains are assessed against authoritative criteria and map to existing register rows or an explicit no-current-gap rationale.
- The prioritized register covers the exact research crosswalk for all P0, P1, P2, validation, security, decision, and assumption items with stable IDs, every requested tracking field, one allowed owner role per row, and `Benchmark maintainer` named as steward.
- Existing manuscript, test, CI/reproducibility, requirements, and roadmap documents are linked as owners; their volatile tables and status prose are not duplicated.
- Every literal repository-evidence occurrence is one canonical in-root tracked path/range, every dirty cited file has a current content hash, and each cited range has been semantically inspected.
- Every local link/anchor resolves; every external source's final destination and identity is confirmed; and every automation-blocked URL is explicitly recorded as `manual_required` with manual evidence.
- `docs/index.md` contains exactly one Reference Docs routing-table link, and a global before/after snapshot proves no out-of-allowlist path changed while preserving unchanged pre-existing user work.
- Documentation whitespace and credential-pattern checks pass across both files, with Hugging Face/bearer forms covered and any match reported only as redacted file/line/pattern metadata.
</success_criteria>

<output>
Create `.planning/quick/260901-0fd-audit-survarena-s-benchmark-readiness-ag/260901-0fd-SUMMARY.md` when done.
</output>
